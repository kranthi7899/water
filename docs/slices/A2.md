# Slice A2: daemon, gateway, conversation plane

Run this on branch `feat/ceo-twin`. Read `CLAUDE.md`, `docs/CONTEXT.md` and `docs/EVOLUTION_PLAN.md` first (the A1 log entry explains the gate, permits and approvals you build on). Work unattended. Make the reasonable call instead of asking, and record the notable ones in the plan's log.

## Goal
A single long-running `water daemon` owns the gate, the approval queue, the audit log, the store and the model sessions. Every client talks to it over a local socket. The conversation plane must feel fast on the Claude **subscription** alone. There is no metered API (see CLAUDE.md).

## Rules
- Stay in A2. No real connectors (A3), no morning brief (A4), no Swift (B).
- The dependency allowlist, zero-metered rule, test gates, per-phase commits and pushes are all in `CLAUDE.md`. End commit messages with `Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>`.
- Tests never spawn the real `claude` binary. Use `backend.Fake` or a fake-CLI test helper that emits stream-json. Check how `internal/backend/timeout_test.go` fakes the CLI and follow it.

## Phases (commit and push after each)

### 1. Streaming provider and warm session (`internal/backend`)
- **Streaming.** Add a `Streamer` interface: `RunStream(ctx, Request, onDelta func(string)) (Response, error)`.
  - For `ClaudeSubscription`, add `--include-partial-messages` to the existing stream-json args.
  - Parse `stream_event` / `content_block_delta` / `text_delta` lines as they arrive, instead of buffering stdout (today `exec.go` buffers). Keep scrubbed env, process-group kill, timeouts and rate-limit parsing intact.
  - Only use flags that the existing `--help` capability parsing (`exec.go`) reports as present. Degrade to non-streaming otherwise.
- **Warm session.** `WarmSession` keeps one `claude --print --input-format stream-json --output-format stream-json --verbose` process alive and writes each user turn as a stream-json user message on stdin.
  - One turn is in flight at a time.
  - Restart it on crash, on a model or system-prompt change, after N turns, or on `/clear`.
  - If the process dies mid-turn, the turn fails cleanly and the next turn cold-starts.
  - Fall back to per-turn spawn if a warm start fails.
- **Model tiers.** `twin.yaml` gains `models: {fast: ..., strong: ...}`, and the provider picks by tier. Default `fast: haiku` and `strong: ""` (CLI default). If the CLI rejects a model, fall back to the default and log it once.
- **Tests:** deltas arrive in order before the result; cancel kills the process group; a warm session reuses one process across two turns and restarts after a crash; tier selection.

### 2. Runtime and daemon (`internal/runtime`, `internal/gateway`, `water daemon`)
- **`internal/runtime`: the turn loop.**
  - The context assembler builds the system prompt from `twins/ceo/role.md` (phase 4 writes it; tolerate it being missing until then), a compact state summary from the store (today's events, pending approvals), and long-term memory.
  - It streams the reply.
  - For `channel=voice` it also emits whole **sentences** (a sentence splitter in code) and instructs short replies.
- **Deterministic fast paths**, with no model call. A small conservative intent matcher in code answers from the store:
  - today's or tomorrow's schedule
  - pending approvals
  - "what's my brief" (say it's not ready yet if it's missing)
  - Anything it doesn't match clearly goes to the model. Table-test the matcher, including near-misses that must fall through.
- **Model-initiated tool calls go through the gate, which lives in the daemon.**
  - Add a twin mode to `water mcp-serve`. Its tools are the manifest's connector functions, and each call is proxied to the daemon over the socket with a per-turn token. The gate, rate windows and audit all stay in one process.
  - Origin is P0 for a user turn.
  - **Taint is computed in code:** if the turn's context contained any record flagged external/untrusted, every tool call in that turn is tainted.
  - An A-level call creates an envelope in the queue and returns "queued for approval <id>" to the model. It never executes inline.
- **`water daemon`**
  - Serves HTTP over a Unix socket at `~/.water/run/water.sock`: directory 0700, socket 0600.
  - Only one instance runs, enforced by a flock on `~/.water/run/daemon.lock`.
  - Shuts down cleanly on SIGTERM.
  - **Auth:** a bearer token per client. The daemon keeps a 0600 `~/.water/run/clients.json`. The CLI uses a `cli` token created on first start, and `water daemon token new <name>` makes more (for the Shortcut or Swift client later). Unknown or missing token → 401.
- **Endpoints:**
  - `POST /v1/turns`: NDJSON stream of `ack`, `delta`, `sentence`, `approval_required`, `done` and `error` events, with `channel` = cli | voice | text-bar.
    - *(Review pass, 2026-09-24.)* `channel` is case-insensitive; empty means cli; any other value is a 400 before the stream starts. Only voice gets `sentence` events.
    - The daemon runs one model turn at a time. `approval_required` is best-effort: it goes only to the stream of the turn whose model call queued the approval, while that call runs. It carries `approval_id`, `action` (also in `text`, for older clients), `risk` and `payload_hash`, which is enough to decide it. Approvals from anywhere else (`POST /v1/decisions/{id}/email`, the agent-mail watcher, a tool call that lands after the stream closed) show up only in `GET /v1/approvals`. A client should refresh that list on `done`.
    - Tool-call taint belongs to the daemon session, not the turn. Once any turn, meeting segment or tool result brings in untrusted content, it stays escalated for the daemon's lifetime, and `/clear` doesn't reset it. After that, S-level calls answer "queued".
    - *(Review pass, 2026-09-24.)* `approval_required` also carries `read_back`: the code-built text (`approvals.ReadBack`) to speak or show before asking yes or no. A client must use it as is and never have a model compose it.
  - `GET /v1/approvals`: pending envelopes, as a JSON array of approval views.
    - *(Review pass, 2026-09-24.)* An approval view has snake_case keys: `id`, `action`, `recipient`, `payload`, `evidence_refs`, `risk`, `origin`, `expires_at`, `payload_hash`, `status`, `reason`, `created_at`, plus the code-built `read_back` (the full text for a yes/no) and `summary` (the short list form). Before this pass the envelope keys were PascalCase.
  - `GET /v1/approvals/{id}` *(review pass)*: one approval view, in any status; 404 if unknown.
  - `POST /v1/approvals/{id}/decision`: the body must include the payload hash the client was shown. If the hash doesn't match, it's refused.
    - *(Review pass, 2026-09-24.)* The response's `envelope` is an approval view. `executed: true` (with `output`) means the action ran even if `error` is also set (for example, its audit record or result indexing failed afterward). `outcome_unknown` means it may have run. Never offer to retry either one.
  - `POST /v1/tools/invoke` (the model-tool bridge): `status` is `ok`, `queued` (`approval_id`), `denied` (`reason`, nothing ran) or `executed_with_error` (`output` and `error`: the action ran, and only its audit record or indexing failed).
    - *(Review pass, 2026-09-24.)* `reply` is free text matched deterministically. The response's `answer` echoes how it was read (`yes` | `no` | `ambiguous`). No and ambiguous are both a final denial, with no "ask again" state, so a voice client should confirm the transcript before posting it.
    - A decision that could not apply (the envelope isn't pending, has expired, or another decider won) answers 200 with the current envelope and `error`. A store or audit failure answers 500.
  - Client tokens: a token minted by `water daemon token new` while the daemon runs is accepted at once. The daemon re-reads `clients.json` when it sees an unknown token, and every write to the file is atomic.
  - `GET /v1/state`
  - `POST /v1/tasks/{id}/cancel`
  - `GET /v1/health`
- **Persistence and audit anchoring** (A1 left these open):
  - Persist gate rate windows and usage counts in the store so they survive restarts.
  - Anchor the audit log's last seq+hash in the store on every append. On open, the file's tail must match the anchor, so a truncated tail is detected.
  - `water audit verify`.
  - `water audit repair`: may only drop a single final unparseable (torn) line, and appends a `repair` entry recording it.

### 3. Clients
- `water ask "<text>"` streams one turn to stdout. With `--voice`, it speaks each `sentence` event through the existing `voice.OS` provider (`say`) as it arrives. That is the free interim voice path the macOS Shortcut will call.
- `water approve` lists pending approvals as a numbered menu, using the A1 CLI rendering and the code-generated read-back, then decides them.
- `water chat` becomes a daemon client talking to the one twin. Keep the editor, theme and slash commands that still make sense.
- If the daemon isn't running, clients say how to start it. They do not run an in-process fallback.
- `water daemon install` / `uninstall`:
  - Writes `~/Library/LaunchAgents/com.water.daemon.plist` with KeepAlive and logs in `~/.water/logs/`.
  - Resolves the absolute path of `claude` at install time into the plist's environment (launchd's PATH is minimal).
  - Uses `launchctl bootstrap gui/$UID`.
  - Unit-test the plist generation. Real loading is checked on the Mac.

### 4. Delete the council (amended by the owner, 2026-09-23)
The multi-agent system is **deleted outright, not kept dormant**. Water is a
single personal agent from here on; there is no persona archive.

- Delete the whole `agents/` directory (ceo, coo, cto, design: souls,
  experience, reasoning, `.index.json`, skills, memory seeds, role.yaml) and
  its `go:embed` line. Nothing is archived; the persona files stay in git
  history only. The single agent's definition lives in `twins/ceo/` —
  write `role.md` there, describing the CEO **role's responsibilities and
  environment** (decisions, briefs, budgets, priorities, investor
  communication, the projects/people/tools it sits among), **not a
  personality**, with an environment stub for the CEO to fill in: company,
  projects, people, tools.
- Delete the council and multi-agent machinery entirely:
  - `internal/orchestrator`: routers, the state graph, `AgentMessage`/outbox,
    **and** the executor and checkpointer — pipelines in later slices are
    plain Go functions, not a state graph.
  - `internal/agent`: whatever the twin runtime still needs (prompt assembly,
    the single call path) moves into `internal/runtime` first, if anything
    still does; delete the rest.
  - `internal/roles`, `internal/persona` (incl. the skills loader),
    `internal/identity` (role_id/content_hash stamping and the HMAC
    keyring), `internal/experience`, `internal/diagnose`,
    `internal/dashboard`, `internal/surface`.
  - `internal/trace` and `water replay`: the audit log and session
    transcripts replace them.
  - CLI commands: `run`, `orchestrate`, `build-site`, `diagnose`,
    `dashboard`, `persona`, `experience`, `skills`, `replay`, `debug dump`.
  - Slash commands `/consult`, `/switch`, `/agents`, `/skills`, `/why`, and
    anything else assuming several roles.
- Collapse per-role plumbing to one agent: memory and sessions become
  single-twin; keep one theme (the CEO's) and one voice; config drops
  per-role backend/model maps (model tiers come from twin.yaml); remove
  every hard-coded role slug.
- Guard tests: delete the ones that only guard the council (singleton,
  part6, evidence, memory isolation between roles, consult). Keep or port
  the ones that still apply: strict MCP config and `--tools ""`, root
  confinement, the metered-leak / `ScrubbedEnv` checks, and the A1 gate
  guards.
- Keep the `internal/tools` pieces the twin still uses: the MCP server in
  twin mode, `ResolveWithinRoots`, atomic write. Delete the rest if it's
  unused, including the old `ApprovalBroker` (the daemon's queue replaces
  it). Leave no dead code or compatibility shims.
- Update `docs/EVOLUTION_PLAN.md`'s keep/move/retire table (the council is
  deleted, not dormant; the orchestrator is deleted; pipelines are plain
  Go) and `docs/CONTEXT.md` (an amendment noting the same, plus that
  twin-to-twin messaging, slice E, is built fresh on its own envelope).
  Update `README.md` to describe a single personal agent.

## Acceptance tests (required)
- A streamed turn over the socket delivers deltas before `done`, against a fake provider.
- A voice-channel turn emits `sentence` events.
- A fast-path schedule question answers from the store with **zero** provider calls.
- Cancel stops an in-flight turn, and the process group is gone.
- A bad or missing token gets a 401.
- The socket is 0600 and a second daemon refuses to start.
- An A-level tool call from the model lands in the approval queue and does not execute. Deciding it with a stale payload hash is refused. Deciding it with the right hash executes it exactly once, and the audit log shows the whole path.
- A tainted turn can't auto-run S-level calls.
- Rate and usage counts survive a daemon restart.
- A truncated audit tail is detected, and `repair` fixes only a torn final line.
- `go vet`, `go test -count=1 ./...` and `CGO_ENABLED=0 go build ./cmd/water` are green.

## Finish
- Append an "A2 done" entry to `docs/EVOLUTION_PLAN.md`: what was built, the design choices, and **what to verify on the Mac**. Include measuring time-to-first-token for a warm session versus a cold spawn, and whether the subscription accepts `--model haiku`.
- Set the plan's status line to "Next: Slice A3".
- Commit and push.
- Final report under 400 words: commits, push status, test results, deviations, and anything left undone.
