# Evolution plan: council to CEO twin

Status: **Slice C write increment done (2026-09-24).** Outbound mail and calendar writes now exist (`gmail.draft_message`/`send_message`, `gcal.create_event`/`move_event`, all level D/A, gate-approved), sending unmistakably as the agent via a verified Gmail alias, never impersonating the CEO — see that entry's own bullets for the idempotency design (`gapi.PostJSON`/`ErrSendOutcomeUnknown`), the forward-function decision, `internal/agentmail`'s inbound-triage watcher, `internal/reports`' wiring, and what still needs the owner (`docs/agent-identity-setup.md`). Before that: Slice M (live-meeting assistance — session lifecycle, prefetch, an FTS5 local index, on-demand help, quiet proactive cues, after-meeting recap), an interim macOS client (Slice B), Slice C-base's decision-registry/generic-frame/classification-trigger layer, a whole-codebase adversarial review (44 confirmed defects fixed), and an opt-in demo slice (fake GitHub/Linear/HubSpot connectors) — see the dated log entries below. Context: `docs/CONTEXT.md`.

## Constraints decided with the owner
- **Zero metered spend.** Model calls go through the Claude subscription (`claude` CLI) only. Voice is free and on-device: Apple `SFSpeechRecognizer` (on-device recognition) and `AVSpeechSynthesizer` / `say`. Twilio, the X API and paid speech services are deferred.
- **Latency comes from architecture.**
  - A warm `claude` stream-json process stays running between turns.
  - Replies are spoken sentence by sentence as they stream.
  - Schedule, brief and approvals questions are answered from the store without a model call.
  - Longer work gets an instant "on it" and runs in the background.
  - Targets: about 1s for state-backed answers, 1.5–3s when the model is needed.
- **Subscription usage windows replace a dollar budget.** Auto mode (P2) gets a per-window call cap, fed from `backend/ratelimit_store.go`.
- **Execution:** one remote cloud run per slice, reviewed before the next. Opus runs A1 (the security core). Sonnet runs the rest.

## Where the prompt's assumptions differ from the repo
- There is no Steve Jobs material in the repo or its history. "Persona material" means the CEO's `soul.md`, `experience.md`, `reasoning.md` and `.index.json`.
- There is no Researcher role. The dormant roles are COO, CTO and Design.
- Nothing streams today. `backend/exec.go` buffers the whole CLI output, and `backend/api.go` doesn't stream.
- There is no daemon, no Keychain code and no connector layer. The only Unix socket is the per-session approval broker.

## Keep / move / retire

**Amended by the owner, 2026-09-23 (during A2):** the council is **deleted
outright, not kept dormant**. Water is a single personal agent. The table
below reflects what actually happened, not the original A1-era plan.

| Existing | Fate |
|---|---|
| `agents/` (all four roles: souls, experience, reasoning, skills, memory seeds, role.yaml, `.index.json`) | **Deleted**, not archived. The CEO's role — responsibilities and environment, not a personality — lives in `twins/ceo/role.md`. Persona files survive only in git history. |
| `orchestrator` (executor, checkpointer, both routers, the state graph, `AgentMessage`/outbox) | **Deleted entirely**, including the executor and checkpointer. Pipelines (brief, recap, packets) will be plain Go functions in a later slice, not a state graph. |
| `agent` (`Assemble`, `call`, `RunTurn`, `Consult`, `ceoNode`/`cooNode`/`specialistNode`) | **Deleted.** `internal/runtime` (built in A2) was written fresh against the twin/gate/store shape and never depended on this package, so nothing needed porting. |
| `roles`, `persona` (incl. the skills loader), `identity` (role_id/content_hash + the HMAC keyring), `experience`, `diagnose`, `dashboard`, `surface`, `trace` | **Deleted.** The audit log and session/turn events replace `trace`; there is no per-role anything left to register, sign, or diagnose. |
| `chat`, `editor`, `session` | **Deleted.** Both were reachable only through the deleted `run`/`build-site` commands and did not compile against a council-free tree; `water chat` is now a plain daemon-client REPL (`internal/cli/cmd_chat.go`), and transcript persistence is left for a later slice. |
| `backend` | Kept. Model tiers, `RunStream`, and a warm session process were added in A2. The API backend stays opt-in only. |
| `tools` | Trimmed to what the twin uses: the MCP server (now in twin mode, proxying connector calls to the daemon's gate), `WritePolicyFile`/`LoadPolicyFile`, and `ResolveWithinRoots`/`ExpandRoots` (kept for a future connector, unused today). Deleted: the interactive-workspace read/write/run/apply_actions/open_page tools, the `ApprovalBroker` (the daemon's queue replaces it), and the shell sandbox. |
| `gate`, `approvals`, `audit`, `store`, `connectors`, `vault` (Slice A1) | Kept, extended in A2: rate/usage windows and the audit anchor persist in the store; `water audit verify`/`repair`. |
| `memory`, `config`, `auth`, `voice`, `guards` | Kept. `memory` is not yet wired into the twin's runtime (a later slice); `config` still carries some now-unused per-role/orchestration knobs (harmless, not cleaned up); `guards` lost the council-only tests and kept/ported the rest (strict MCP config, `--tools ""`, metered-leak/`ScrubbedEnv`, the A1 gate guards). |
| `theme`, `layout`, `themes/*.yaml`, `water.ThemesFS()` | **Deleted in A3.** Unwired dead weight since the council was deleted in A2; nothing referenced them outside their own tests. |
| `run`, `orchestrate`, `build-site`, `diagnose`, `dashboard`, `persona`, `experience`, `skills`, `replay`, `debug dump`, `memory` (CLI) | **Deleted.** `/consult`, `/switch`, `/agents` went with the TUI that had them. |

The new layout follows the tree in CONTEXT.md. Two approved dependencies are added, both pure Go so the binary stays static: `modernc.org/sqlite` and `modelcontextprotocol/go-sdk` (MCP client only). The Keychain is reached through the `/usr/bin/security` CLI behind a `Vault` interface, so it needs no cgo.

## Slices and acceptance tests
Every phase must pass vet, test and a `CGO_ENABLED=0` build.

1. **A1 (remote, Opus): foundation.**
   - Build: `gate` with R/D/A/S/B, default deny, untrusted tagging, rate and usage caps; `approvals` (payload-hash envelopes, a persisted queue, code-generated read-backs, a deterministic yes/no matcher); `audit` (hash-chained, 0600, fails closed); `store` (SQLite, migrations, eight record types with source IDs); `connectors` (the contract plus a fake connector); `vault`.
   - Tests: an A-level call without an envelope is refused; an edit voids the approval; untrusted-derived parameters are refused at S; every call is audited and the chain verifies; caps trip; tokens never appear in prompts or logs.
2. **A2 (remote, Sonnet): daemon and conversation plane.**
   - Build: `water daemon` on a 0600 Unix socket with per-client tokens; streaming turns (cli, voice, text-bar); approvals, state and cancel endpoints; `water chat` and `water ask` as clients; a LaunchAgent install command; `RunStream`, the warm session process and deterministic fast paths; retire the council wiring.
   - Tests: streaming, cancel, bad-token rejection, socket permissions.
   - On the laptop: measure time to first token.
3. **A3 (done): Google Workspace (Calendar, Gmail, Drive/Docs/Sheets), read-only.**
   - Remote: code and fixture tests.
   - Laptop: an internal GCP app, OAuth consent, and the live test suite.
4. **A4 (remote): morning brief pipeline.** Ranking signals are computed in code, and the model only writes the text.
5. **B (laptop-heavy): voice and clients.**
   - A Swift menu-bar app with a push-to-talk hotkey and a text-bar hotkey. Replies are spoken sentence by sentence, and approvals are confirmed by voice.
   - Interim: a macOS Shortcut that calls `water ask`.
   - A Slack connector.
6. **C (remote): decisions and planning.** Playbooks (budget, investor, hiring) and packets with a readiness state; the meeting-recap recipe; the action-plan scheduler; the four validated templates; the P0/P1/P2 queue with auto mode; the learning log.
7. **D: remaining connectors.**
   - In order: transcripts (Meet first), GitHub, Linear/Jira, HubSpot, QuickBooks sandbox, LinkedIn, file creation.
   - Remote: code. Laptop: credentials.
8. **E (remote): manifest generalization and twin-to-twin loopback.** Includes a second twin that needs no code changes.

**Deferred:** Twilio and X (both cost money), project health rubrics, dependency linking, the department layer, a phone client, wake word, and barge-in.

**Deferred, tracked separately (owner's long-term concern, 2026-09-23): host the daemon off the laptop.**
Right now `water daemon` only does anything while the Mac is awake (macOS suspends background processes on sleep), so background sync and Gmail push (once built) only work while the laptop is up. That's fine for now but won't scale to "always available." The eventual fix: run `water daemon` on a small always-on machine (e.g. a Mac mini, since it still needs to run the `claude` CLI logged into the subscription), with the laptop, phone, etc. becoming clients that just talk to it over the network instead of running it locally. Not scheduled to any slice yet; revisit once the core agent loop (through Slice C) is solid.

## Log
- 2026-09-23: Step 0 done. Reasoning-layer work committed on `feat/build-site`, branch `feat/ceo-twin` created, and CONTEXT.md, CLAUDE.md and this plan written.
- 2026-09-23: **A1 done.** New packages: `store`, `audit`, `canon`, `twins` (with `twins/ceo/twin.yaml`, embedded), `gate` (+ `gate/permit`, `gate/internal/mint`), `connectors` (+ `connectors/fake`), `approvals`, `vault`. The council code is untouched.
  - **How the gate is made unbypassable.** A connector's `Invoke(ctx, permit.Permit)` gets its arguments and credential only by redeeming the permit (`p.Open()`), and it can do that once. Permits are minted in `internal/gate/internal/mint`, which Go's internal-directory rule lets only `internal/gate/...` import. `gate/permit` re-exports the type, so connectors can accept a permit but cannot make one. The gate also fails any call where the connector returned without redeeming. A guard test checks all of this, and also scans the source to confirm that only the gate imports the mint or calls `Invoke`, and that every connector redeems as its first statement.
  - **Other choices:**
    - Taint's zero value is "unknown" and counts as tainted.
    - A tainted call to an S function escalates to needing an approved envelope. Tainted R and D calls are allowed because nothing leaves.
    - A manifest may grant a function's declared level or tighten it to A or B, never loosen it. `gate.New` enforces this.
    - Approvals bind the sha256 of the canonical payload and must also match the action. A mismatched call voids the approval. Claims are compare-and-set, and a stored row whose payload no longer matches its hash is refused.
    - Every audit write comes before the effect it records, and an audit failure fails the action. An approval that can't be audited is reverted to denied.
    - `audit.Open` refuses to extend a chain that doesn't verify.
    - Rate and usage windows are in memory for now, so the A2 daemon holds them.
    - P2 model calls pause while the persisted subscription status isn't "allowed".
    - Added one audit kind, `propose`, for a new envelope entering the queue.
  - **Known limit:** truncating the tail of the audit log isn't detectable without an external anchor for the last hash. Consider one in A2.
  - **Verify on the Mac:** `WATER_KEYCHAIN_TEST=1 go test ./internal/vault`. It passed during the run, but check it from a normal login session, and check that reads by `/usr/bin/security` never prompt. Secrets must be single-line because they go in on stdin.
- 2026-09-23: **Owner decision, mid-A2:** the council is deleted outright, not kept dormant. Water is a single personal agent from here on; there is no persona archive. This replaced Phase 4 of `docs/slices/A2.md` and is reflected in the keep/move/retire table above and in `docs/CONTEXT.md`'s amendment.
- 2026-09-23: **A2 done.** `water daemon` (HTTP over a 0600 Unix socket, single-instance flock, bearer-token clients) now owns the gate, approvals, audit, store and model sessions; `water chat`, `water ask` and `water approve` are its clients. New: `internal/runtime` (context assembly from `twins/ceo/role.md` + a state summary, a phrase-based zero-model-call fast path for schedule/approvals/brief questions, sentence-by-sentence voice streaming), `internal/gateway` (the daemon itself, plus a Twin extension to `internal/tools` that proxies model-initiated connector calls from an MCP-serve child to the daemon's gate). `internal/backend` gained `RunStream`, `WarmSession` (one persistent `claude` process reused across turns, restarting on crash/system-prompt/model/tool-scope change or after 40 turns) and model tiers from `twin.yaml`. Deciding a pending approval "yes" now executes the action in that same call, exactly once. Gate rate/usage windows and the audit log's tail now persist in the store (`water audit verify`/`repair` round out A1's open items). Then the council was deleted per the owner's decision above: `agents/`, `orchestrator`, `agent`, `roles`, `persona`, `identity`, `experience`, `diagnose`, `dashboard`, `surface`, `trace`, `chat`, `editor`, `session`, and the CLI commands that only served them.
  - **The stable-token deviation.** The spec sketch implied a token per turn for the model's tool-proxy auth. That cannot work with a warm session: the MCP bridge child a warm process spawns reads its `--mcp-config` policy file once, at its own startup, and then serves every turn in that process's life — so the token baked into it cannot rotate per turn without forcing a process restart on every single turn, which defeats the warm session. A2 instead mints one long-lived, session-scoped proxy token, and escalates its taint (never resets it) the first time any turn's assembled context includes external content. This is the conservative direction to err in given the constraint.
  - **Verified once by hand against the real `claude` CLI** (never in tests, which use `backend.Fake` or a fake-CLI shell script): streaming deltas and voice-channel sentences, `--include-partial-messages` actually needed adding to the warm session's own args (a bug caught only by this manual check — the automated warm-session tests use a fake CLI that doesn't need the real partial-message shape), a model calling `fake_calendar__list_events` and `fake_calendar__create_event` over MCP, the `create_event` call landing in the approval queue, and approving it executing exactly once. Cold spawn vs. warm reuse measured on this Mac: about 1.47s vs. 0.77s for a trivial turn.
  - **Left undone / for later slices:** `water memory` has no replacement command and long-term memory is not wired into the runtime yet; transcript persistence for `water chat` (the old per-role `session` package was deleted, not replaced); `theme`/`layout`/three of the four `themes/*.yaml` are dead weight, not deleted; `config` still carries unused per-role/orchestration knobs; the model-tool bridge's "session taken as one unit" taint model should be revisited once P1/P2 scheduling (auto mode) exists and more than one logical conversation can be in flight.
  - **Verify on the Mac:** `water daemon install` and `launchctl` loading it (unit-tested only as plist rendering); that a second `water daemon` truly refuses to start when the first is running under launchd, not just in-process; time-to-first-token specifically (this run measured whole-turn latency, not first delta) for warm vs. cold.
- 2026-09-23: **A3 done.** The twin can now read the CEO's real Calendar, Gmail and Drive, read-only, in place of the `fake_*` connectors. Built as a workflow: one Opus foundation phase, three connectors and a cleanup pass in parallel, then this integration pass.
  - **New packages.** `internal/connectors/google/gapi`: one Keychain credential (`water.google/<account>`, single-line JSON) shared by all three connectors, an in-memory access-token cache keyed by a hash of the credential (refreshed ahead of expiry or once on a 401, with truncated exponential backoff on 429/5xx/rate-limited-403), the PKCE loopback `Authorize`/`Revoke` flow, and scrubbing (exact-string plus a regex over `ya29.*`/`1//*`/`GOCSPX-*`/`4/*`) so no token ever reaches an error, the audit log or a store row. `gcal` (`list_events`), `gmail` (`list_messages`, `get_message`), `gdrive` (`search_files`, `read_file`) — every function `External: true`. Docs/Sheets/Slides read through Drive's `files.export`, so no scopes beyond the three read-only ones are needed.
  - **Wiring.** `twins/ceo/twin.yaml` now lists only the six Google functions (level R, per-function rate caps); `auto_allowlist` holds just `gcal.list_events` and `gmail.list_messages`. The `fake_*` connectors left the production manifest entirely — they now live only in the gate's, guards', and gateway's own inline test manifests (those tests already defined their own manifests and needed no changes; two tests that loaded the *embedded* `twins/ceo/twin.yaml` — `TestEmbeddedCEOManifestLoads` and `TestEmbeddedCEOManifestBuildsAGate` — were updated to check the real Google functions instead of `fake_mail.*`).
  - **`water connect google --client-file <path> [--account] [--status] [--revoke]`** runs the loopback flow (prints the URL and opens it with `/usr/bin/open` on macOS), stores the credential, and never prints a secret. `--status` forces a live refresh; `--revoke` revokes at Google and deletes the Keychain entry. `water doctor` reports connected/not-connected by presence only (no network call, so `doctor` stays fast and offline-safe).
  - **Background sync (`internal/sync`).** The daemon starts one `Refresher` tied to its own shutdown context: once right after start and then every `sync.interval_minutes` (default 10), it calls `gcal.list_events` (today through +7 days) and `gmail.list_messages` (`newer_than:1d`) through the gate at origin **P2** with **Clean** taint. It checks the vault for the shared credential before calling anything, so an unconnected twin logs one line instead of a stream of gate denials every interval. Its function ids and the credential it checks for are both overridable, so tests exercise it entirely with the `fake_*` connectors and a memory vault — no network, no real Google.
  - **The taint fix.** A2 built `escalateTaint` (sticky, never resets) but never called it from the one place that matters most: `handleToolInvoke`, the model's own tool-call path. A3 makes an `Untrusted` gate result there escalate the session's stable tool-proxy token, so a model that just read an email is tainted for every later call in that session, not only for taint computed once at context-assembly time.
  - **The fast-path fix.** `scheduleAnswer` and `runtime.StateSummary` used to `List` the 500 newest records by `created_at` and filter by `start_at` in Go — a background sync backfilling an old event, or just enough other records accumulating, could push a today-starting event out of that window. Added `store.EventsInRange(ctx, s, from, to)`, a direct `start_at` range query with no dependency on insertion order, and pointed both call sites at it.
  - **Cleanup, done alongside this slice by a parallel agent:** `internal/theme`, `internal/layout`, `themes/*.yaml` and `water.ThemesFS()` deleted (dead since the council left in A2); `internal/config` and `internal/voice` collapsed to what a single CEO twin actually uses (no more COO/CTO/design voices; old config files with the retired orchestration/sessions/tools/skills/ui/telemetry/memory keys and per-role voices still load, silently dropping them).
  - **Left undone / for later slices:** incremental sync (Calendar's `syncToken`, Gmail's `historyId`) — A4 territory, since the morning brief needs exactly that kind of "what changed" signal; write functions (`send_email`, `create_event`) are C/D; Docs/Sheets-specific APIs beyond `files.export` (comments, cell ranges) are undone.
  - **Verify on the Mac:** the whole of `docs/google-setup.md` end to end on a personal Gmail account — the consent screen's "In production" + unverified path really does avoid the 7-day Testing-mode token expiry; `water connect google --status` after a real day has passed; `water daemon` running long enough to see two sync intervals fire and check `water audit verify` afterward; `water ask "what's on my schedule today?"` answering from real data with zero model calls; `water ask "summarize my unread email from today"` actually calling the Gmail tools (visible in the audit log) rather than refusing or hallucinating.
- 2026-09-24: **A4 done.** Incremental sync for both Google connectors, a split two-interval background refresher, and the morning brief pipeline. Built as a workflow: a store-foundation phase, the two connector extensions in parallel, then this integration pass.
  - **Owner decision, recorded so it isn't re-litigated: no Gmail push.** Google's Pub/Sub push notifications need a billing account linked to the (free) Google Cloud project, plus one more permission re-approval, for roughly a minute of latency gain over a 60-second poll. The owner explicitly chose fast polling over push on 2026-09-24. A4 adds no Pub/Sub, no new OAuth scope, and no new dependency — it stays inside the existing hand-rolled-REST, GET-only pattern.
  - **`gcal`/`gmail` incremental support.** `gcal.list_events` gains an optional `sync_token` (mutually exclusive with `time_min`/`time_max`); a successful call of either shape now returns `{"events": [...], "next_sync_token": "..."}` instead of a bare array, and a 410 from Google becomes the typed sentinel `gcal.ErrSyncTokenExpired`. `gmail.list_messages` gains an optional `since_history_id`, walking `users.history.list` (`historyTypes=messageAdded`) instead of searching; output becomes `{"messages": [...], "history_id": "..."}`, and a 404 on a too-old `historyId` becomes `gmail.ErrHistoryTooOld`. Both changes are additive when the new argument is omitted (the underlying HTTP request is unchanged), but the output shape change is a deliberate, unavoidable break of the old bare-array bytes, needed to surface the cursor to callers — `Normalize` was updated for both, and both packages' full existing test suites still pass.
  - **Cursors (`internal/store`).** New migration `0003_sync_cursors.sql`: `sync_cursors(key, value, updated_at)` and `briefs(day, text, created_at)`, with `GetCursor`/`SetCursor`/`DeleteCursor` and `GetBrief`/`SetBrief` mirroring `gatestate.go`'s existing upsert-by-key pattern. Cursor keys follow a `connector:scope:field` convention (`gmail:history_id`, `gcal:primary:sync_token`); `briefs` is keyed by local calendar day (`YYYY-MM-DD`).
  - **The gate's redaction was silently discarding typed sentinel errors.** `gate.Invoke` scrubbed a failed connector call's error with `errors.New(redact(...))`, which drops the original error's identity — so `errors.Is(err, gcal.ErrSyncTokenExpired)` (or any other connector sentinel, pre-existing ones included) never matched the error `gate.Invoke` actually returns, contrary to A3's assumption that redaction "only strips credentials, not typed sentinel errors." Fixed with a small `scrubbedError` type that returns only the redacted text from `Error()` (so a credential still can't resurface through formatting) but implements `Unwrap` to keep the original error reachable for `errors.Is`/`errors.As`. Caught and fixed in this integration pass, since both connector-extension agents flagged it as a blocker for the sync loop's planned fallback logic but were (correctly) scoped away from touching `internal/gate` themselves.
  - **`internal/sync` split into two independent tickers.** `MailInterval` defaults to 60s (Gmail's per-user quota — 6000 units/min, ~2 units per `history.list` — makes this trivial); `EventsInterval` keeps the existing 10m default (`sync.interval_minutes`; new `sync.mail_interval_seconds` for mail). Each tick loads its own stored cursor, calls its function with it, and on success persists whatever cursor field the response carries. On the connector's expired-cursor sentinel (matched via `errors.Is` through the gate), the stored cursor is deleted and logged; the next tick is then naturally cursor-less, does a full fetch, and re-seeds a fresh cursor from that response — the same fallback the connectors already do unprompted, just triggered by an empty cursor rather than special-cased in the loop.
  - **Morning brief (`internal/runtime/brief.go`, new).** Signals — today's events, new messages since yesterday (by `created_at`, via the existing generic `store.List`), a simple non-model "needs a reply" heuristic (excludes obviously bulk/no-reply senders, then looks for a question mark or a short list of urgency/deadline phrases — documented in code, not meant to be exhaustive), and the pending-approval count — are all computed in code; one `backend.Request` asks the model only to phrase them, instructed never to invent a fact outside the signal block. Cached per local day in the new `briefs` table. `ComputeAndCacheBrief` is a compute-once guard (an in-memory per-day wait map): a background precompute and an on-demand ask racing each other share one backend call rather than each starting their own, verified by a test that fires several concurrent callers and asserts the fake backend saw exactly one. `fastpath.go`'s morning-brief branch calls it — the one deliberate, clearly-commented exception to "the fast path never calls the model." Background precompute reuses the events tick (no new goroutine): once local time is past `brief.ready_after` (default `07:00`) and nothing is cached yet for today, it calls the same function the on-demand path does.
  - **Taint.** Because a cached brief might have been computed by the background precompute long before anyone asks for it, taint is recomputed fresh on every request (cheap: local store reads only) rather than stored alongside the cached text, and escalates the session exactly like a normal turn's tainted context does. This needed one small, deliberate exception to "only touch the files this task named": `runtime.Env` gained an `OnTaint func(bool)` field, wired in `gateway/daemon.go`'s `baseEnv()` to the existing (private) `escalateTaint`, since the fast path has no other way to reach that mechanism.
  - **Left undone / for later slices:** the brief's own "needs attention" heuristic is intentionally simple and non-exhaustive; long-term memory still isn't wired into the brief's signals (it only reads the store); the background precompute's "past ready_after" check uses the sync loop's own clock (`Config.Now`, real time in production) rather than anything more sophisticated (a change in system timezone, a laptop asleep through the whole ready window, etc. are not specially handled — the next tick after wake still checks and computes). Nothing here rechecks the sync loop's assumption that Gmail keeps history for "at least a week" and gcal's `syncToken` lifetime against a real long-idle client — only fixture-driven 404/410 behavior is tested.
  - **Verify on the Mac:** `water daemon`, send a test email, confirm `water ask "summarize my unread email from today"` picks it up within about a minute instead of ten; `sqlite3 ~/.water/water.db "select * from sync_cursors;"` shows a `gmail:history_id` row after the first successful sync; `water ask "what's my morning brief"` computes the first time (a few-second pause) and is instant on a second ask; `water audit verify`.
- 2026-09-24 (overnight, docs only): **Owner amendment for Slice C and a new Slice M, specs written, no code.** The owner's amendment extends Slice C with a flexible decision-type registry and a generic decision frame, and adds Slice M (live-meeting assistance via the Swift client's on-device transcript, never audio, to the daemon). Their own sequencing is explicit — A4 (done) → B → C-base → M, "do not start C or M until the slice before it is verified" — and Slice B itself is still not built (an interim macOS Shortcut, hotkey → dictate → `water ask --voice`, is the only voice path that exists today; the real Swift menu-bar app, text pop-up first then voice, has not been started). This session therefore wrote specs and planning docs only, no Go code:
  - `docs/slices/C.md` — the decision-type registry (`twins/ceo/decisions/*.yaml`, validated at startup), the always-present `generic` frame, the `Classify` triage interface, the decision card shape, and the two shipped types (`generic`, `budget_request`) plus stub registry entries (`investor_request`, `inbound_decision`). Marked blocked on B.
  - `docs/slices/M.md` — the Swift-client/daemon split (system+mic audio and on-device STT stay client-side; only timestamped text segments cross the local API; the daemon never receives audio and never writes it to disk), the meeting-session lifecycle, prefetch at gate level R, a local SQLite FTS5 index, on-demand help as a P0 fast-model request, quiet proactive cues behind a default-off flag, the after-meeting recap reusing the fixture-transcript code path, and a replay mode via the same segments endpoint (scenario S13). Marked blocked on B and C-base.
  - `docs/slice-c-planning.md` — the exact write functions C's next increment needs (`gmail.draft_message`/`send_message`, `gcal.create_event`/`move_event`), the added OAuth scopes (`gmail.compose`, `gmail.send`, `calendar.events` replacing `calendar.events.readonly`), and a worked example showing the first write call would exercise the approval queue and `approvals.ReadBack` — which already special-cases `send_email`/`create_event` by name today, unused until C ships. Also records the P2 gate decision as **decided, not open**: `gate.authorize`'s existing P2 branch already restricts auto mode to allowlisted R/D functions and never level A, by construction, with no code change needed; this stays in force through C. Proposes a lightweight scenario harness (a new test-only `internal/scenario` helper, not a framework) and expands `twins/ceo/role.md`'s existing environment stub into a Company/Projects/People/Budget-structure template with placeholder values. Works out one example scenario (a conference speaking invite) that exercises the generic frame end to end.
  - **Finding: no scenario-numbering convention exists in this repo.** Searched `PROGRESS.md` and the archived decision logs for "scenario S<N>" or a numbered gate-test list; nothing matches. The amendment's "scenario S8" and "scenario S13" are used exactly as given, as this document's own new identifiers, not as continuations of a pre-existing numbered list.
  - **Finding: FTS5 works with the pure-Go driver already in use.** `modernc.org/sqlite` (v1.59.0, already in `go.mod`) compiles FTS5 into its translated SQLite source by default — no loadable extension, no cgo, no new dependency needed for Slice M's local text index.
  - **Housekeeping, same session:** `docs/architecture.md` and `PROGRESS.md` rewritten for the single-twin daemon (the four-role-council content they described no longer exists in the tree); `docs/known-gaps.md` rewritten for the twin's actual current deferrals (Slice C's standing-grants/probabilities/playbook-learning/classifier deferrals, Slice M's deferred consent handling), with its old persona-coverage content archived; `docs/decisions.md`, `docs/tools.md`, `docs/voice.md`, `docs/judgment-pass-v2.md`, `docs/persona-audit-prompt.md`, and both `docs/reasoning-layer-*-prompt.md` files moved to `docs/archive/` (via `git mv`, history preserved) since they describe the deleted council, orchestrator, or TUI and would otherwise mislead a future session.
  - **No .go files touched.** Gates run: `go vet ./...`, `go test -count=1 ./...`, `CGO_ENABLED=0 go build ./cmd/water`.
- 2026-09-24: **Slice B, interim: the macOS client.** `clients/macos/` is a Swift package built without Xcode (`build.sh` → an ad-hoc-signed `Water.app`; `test.sh` → swift-testing, since XCTest needs full Xcode). It has a menu-bar drop icon, a ⌃⌥Space text pop-up bar (`text-bar` channel, deltas streamed), and ⌃⌥V push-to-talk (on-device `SFSpeechRecognizer` only, with each `sentence` event spoken as it arrives). The HTTP-over-Unix-socket client is hand-rolled, and the token lives in the Keychain (`water.client`/`macos`). Verified against the live daemon over the socket. The hotkeys, mic, speech and TTS need the owner's permission grants. Two daemon findings: (1) clients.json is read only at daemon startup, so newly minted tokens 401 until a restart, and the app falls back to the `cli` token until then; (2) `approval_required` is defined but never emitted. Details and the owner's steps are in `docs/slices/B.md`. Still open in B: voice approvals, the Shortcut, and Slack.
- 2026-09-24: **Slice C-base done.** `docs/slices/C.md`'s decision registry, generic frame, classification triage and card builder are now real code, wired into the running daemon, the CLI and the morning brief. Built as a workflow: an `internal/decisions` foundation phase, `budget_request` (the one type exercised end to end) and the classification-trigger/store-cache/stub-types piece in parallel, then this integration pass.
  - **New package `internal/decisions`:** `Registry`/`LoadRegistry` (validates `twins/<id>/decisions/*.yaml` against the manifest — bad YAML, an unknown key, a `needs[].fetch` naming a function the manifest doesn't grant at level R, a duplicate id, and more all fail loudly, exactly like `twins.Load` does for a bad `twin.yaml`); the built-in `generic` frame; `Classifier`/`ModelClassifier` (one fast-tier model call over the registry's `id`+`trigger` index only); `Triager` (in-memory cache, keyed by the item's own `Source`+`SourceID`); `Card`/`Builder` (every number and evidence source traces to a connector result or a registered `internal://` compute function — the builder quarantines anything that doesn't and downgrades `Ready` to `missing_info` rather than let an unsourced figure through); `Phraser`/`ModelPhraser` (the only thing a model may write is prose, and even that is checked field-by-field against the card's own facts before being kept). Shipped types: `generic` (always present, a Go literal), `budget_request` (a real Drive-spreadsheet CSV parse computing runway/burn in code — `internal://budget_request.compute_runway`), and two schema-valid stubs, `investor_request`/`inbound_decision`, proving the registry holds more than one type without building either out.
  - **The design gap the specs left open, filled: WHEN Classify() runs, on WHAT.** Classifying every synced record would be wasteful and expensive. The rule built here: an item is a classification candidate only if `internal/runtime/brief.go`'s existing "needs attention" heuristic already flags it (the same cheap, code-only signal the morning brief already uses to call out messages) — nothing else is ever classified. `decisions.Candidate`/`NeedsAttention` (`internal/decisions/attention.go`) is a deliberate, documented byte-for-byte copy of `brief.go`'s private `needsAttention`, not a call into it: `internal/decisions` cannot import `internal/runtime` (this slice makes `internal/runtime` import `internal/decisions` for the new open-cards signal, so the dependency has to run one way), and `brief.go`'s heuristic is unexported. **Left for a later pass:** delete `brief.go`'s private `needsAttention` and have it call `decisions.Candidate` instead, collapsing the two copies into one — natural to do at the same time something next touches `brief.go` anyway. Each candidate is then classified at most once, ever, via two layered caches: `Triager`'s in-memory cache (dedups within one process) in front of `StoreCache` (`internal/decisions/trigger.go`), which persists the verdict in a new `decision_classifications` table (migration `0004`) keyed by `(Source, SourceID)` — a daemon restart never re-classifies an item it already has a verdict for. `Trigger.Run` only ever persists the *classification verdict*, never the card itself: a card is always rebuilt fresh from current gate/connector state, so a rebuilt runway figure, a newly-arrived comparable request, or a fixed placeholder Drive id all show up on the next look without needing any cache invalidation.
  - **Wiring into startup, the CLI and the brief (this integration pass).** `internal/cli/twin.go`'s `buildTwinDeps` now also calls `decisions.LoadRegistry` right after `twins.Load`, before the store or audit log ever open, so a malformed `twins/ceo/decisions/*.yaml` stops the daemon exactly as a bad `twin.yaml` does (`buildTwinDeps` was split into a thin default-path wrapper and a testable `buildTwinDepsFS(fsys fs.FS)`, so this is covered by a test against a fixture tree rather than only by inspection). `buildDecisionsTrigger` (same file) builds the one shared `*decisions.Trigger` — a fast-tier `ModelClassifier` wrapped in a durable `StoreCache`, feeding a `Builder` over the real gate — once backend selection happens in `cmd_daemon.go`'s `runDaemon`, and hands the same instance to both `gateway.Config.Decisions` and the background brief's `runtime.Env`. **CLI surface: `water decisions list` / `water decisions show <id>`**, mirroring `water approve`'s list-then-act shape: `list` hits a new `GET /v1/decisions` daemon endpoint (runs `Trigger.Run` over today's candidates, returns cards ranked by `decisions.Rank` — severity, then deadline), `show` renders one in full with `Card.Render()`. **Morning brief:** `internal/runtime`'s new `DecisionSource` interface (`Run(ctx, now) ([]*decisions.Card, error)`, which `*decisions.Trigger` already satisfies structurally) lets `brief.go` compute a ranked open-cards signal the same way it computes events/messages — present only when `Env.Decisions` is set and returns at least one card, never forced in as an empty section, and an `Untrusted` card escalates the brief's taint like an external message already does.
  - **A nil-interface trap avoided on purpose.** `gateway.Config.Decisions` and the trigger passed into the brief's `runtime.Env` are concrete `*decisions.Trigger` pointers that can be nil; assigning a nil pointer straight into the `runtime.DecisionSource` interface field would produce a non-nil interface wrapping a nil pointer, so `env.Decisions != nil` checks would misfire even though every call would then hit `Trigger.Run`'s own nil-receiver guard (which does handle it, defensively, but by erroring — not by omitting the signal). Both `baseEnv()` and `cmd_daemon.go`'s `briefEnv` construction only assign the field when the concrete pointer is non-nil.
  - **Tests, this pass:** `internal/cli/twin_test.go` (a malformed decision-type file and an unlisted fetch function both fail `buildTwinDepsFS` before the real store ever opens); `internal/gateway/decisions_test.go` — the end-to-end proof the task asked for: a fixture message with no matching registered type produces a valid `generic` card over the real `GET /v1/decisions` HTTP path (real registry, real trigger, real gate), plus a no-registry-configured case reporting an empty list rather than erroring; `internal/runtime/brief_test.go` gained cases for ranking, the omit-when-empty rule, taint propagation, and a failing `DecisionSource` surfacing its error; `internal/decisions/rank_test.go` unit-tests `Rank`'s severity/deadline/stability ordering directly.
  - **Left undone / for later slices:** `budget_request.yaml`'s `budget_spreadsheet` need still names a placeholder Drive file id (`CONFIGURE_ME_...`) the owner must replace with their real runway spreadsheet's id — until then it blocks to `missing_info`/errors at the gate rather than failing silently, but the type won't actually reach `Ready` against real data first; the scenario-harness proposal and the `role.md` environment-section expansion from `docs/slice-c-planning.md` are planning only, not built; C's write functions (`gmail.draft_message`/`send_message`, `gcal.create_event`/`move_event`) are the *next* increment, not this one — every `StagedAction` this slice can produce is correctly `Actionable: false` ("not yet available") since none of those functions exist in the manifest yet.
  - **No factual errors found in `docs/slices/C.md` or `docs/slice-c-planning.md`** during this pass; neither was changed.
  - **Gates run:** `go vet ./...`, `go test -count=1 -race ./...`, `CGO_ENABLED=0 go build ./cmd/water` — all green.
- 2026-09-24: **Whole-codebase adversarial review, 44 confirmed fixes.** Five Opus reviewers, one per subsystem (security core: gate/approvals/audit/vault/twins; connectors/sync; the daemon/gateway/CLI/backend; the new `internal/decisions`; and a general quality sweep), read every file in their scope and reported 51 candidate findings. Each was then independently re-examined by a separate Opus reviewer instructed to try to refute it; 44 survived. Only confirmed findings were fixed, each with a test proven to fail before the fix and pass after.
  - **Security core (6 fixed):** an S-level call presented with an approved envelope never actually claimed it, so the same approval could be replayed; the yes/no matcher treated a bare "correct" as an affirmative even in phrases like "please correct it" (removed — now ambiguous, i.e. treated as no); the spoken/CLI read-back silently omitted any payload field it didn't special-case by name (a recipient or amount outside the known fields could go unread before approval); a crash between an audit write and its anchor update could brick the log on reopen (now rolls forward one entry and records a `repair`, instead of refusing to start); a failed disk sync could duplicate a sequence number on retry (now truncates back and poisons the log if even that fails, rather than corrupt silently); a denied approval racing a concurrent state change could log a false denial instead of "changed while deciding."
  - **Connectors/sync (7 fixed):** Gmail's incremental history walk could drop mail newer than what it actually read when cut short by the per-call cap (cursor logic reworked to resume from the last fully-processed record, never skip ahead); Calendar's `syncToken` path could get permanently stuck if a single sync window had more changes than the page cap; incremental Calendar fetches had silently dropped `singleEvents` expansion; `rate_hits` rows were never pruned (unbounded table growth); the store was missing indexes on `created_at`/`start_at` that the fast paths and brief already query by; a Gmail list call could overwrite an already-fetched full message body with a shorter snippet; Gmail's history walk didn't filter out spam/drafts the way the original query-based path did.
  - **Daemon/gateway/CLI (15 fixed):** the warm `claude` session could desync its turn accounting on a cancelled call; a warm-session call with no configured timeout could deadlock the whole daemon on one stuck mutex; the first turn after a taint escalation was a silent no-op instead of taking effect; a failing decisions source made the brief fail open (untainted) instead of blocking; `water status`/`doctor`/`audit` could conflict with the daemon over the same lock file; `/clear` could send an empty turn to the model; the warm session could thrash-restart under specific error sequences; daemon shutdown could leak in-flight requests instead of draining them; a warm-session reader goroutine and its policy temp file could both leak on restart; the fast path could match on the wrong trigger phrase and misroute an action request; `approval_required` was defined in the protocol but never actually emitted by anything (now wired); the warm session was missing isolation flags present on the cold-start path; the Codex backend silently dropped inlined attachments.
  - **`internal/decisions` (8 fixed, on top of last session's own review):** `budget_request`'s runway computation could read a truncated 280-character excerpt instead of the real spreadsheet content; the money parser accepted `NaN`/`Inf` as valid figures; the phraser's "every number must be sourced" check could be defeated by spelling a number as a word or via set-membership tricks; `Trigger.Run` could abort early and leave its own concurrency slot permanently consumed; the classification cache had no path back from a permanently-cached `generic` verdict, and `Forget` didn't actually clear it; a decision's sender field reached a Gmail search query unsanitized (a quoting/injection risk in the query itself, not an auth boundary); `StagedAction`s were never validated against the manifest before being shown as "actionable"; card deadlines were never actually populated from any source.
  - **Quality sweep (8 fixed):** `--voice` on the CLI bypassed the configured voice provider entirely; `water config set` didn't validate a value before saving it; a config save could silently skip a migration or a read error; the list of valid config keys was maintained in three separate places; `config set` coerced some string values incorrectly; `internal/voice` still carried a vestigial per-role parameter now that there's only one twin; the OpenAI voice path bypassed the provider registry; a dead code path in config's expansion logic.
  - **A real coordination failure during the fix phase, caught and corrected, not hidden.** The fix phase ran five agents against the *same* working tree with no isolation between them (a workflow-design mistake — genuinely overlapping files like `internal/gate/gate.go` needed edits from more than one batch). Two agents' commits collided: one commit briefly dropped another's rate-limit-pruning fix, and a third agent's commit recovered it from the shared index before anything reached `origin`. This was **not taken on faith** — after the workflow reported completion, the whole suite was re-run from a clean pull: `go vet ./...`, `go test -count=1 -race ./...` (every package, no hang, no failure), `CGO_ENABLED=0 go build ./cmd/water`, `gofmt -l .` (clean), plus a direct read of the recovered pruning code to confirm it was real and correct — then the live daemon was restarted on the rebuilt binary and smoke-tested (`water audit verify`, `water decisions list`, `water ask`) before trusting any of it. **Lesson for the next multi-agent fix pass over shared files: isolate each batch in its own worktree and merge sequentially, don't pipeline concurrent edits against one tree.**
  - **Net change:** 75 files, +4050/-417 lines. All gates green on the actual HEAD commit, independently re-verified, not just reported.
- 2026-09-24: **Slice M done.** Live-meeting assistance: the daemon never receives audio and never writes any to disk, on either channel, and passively-transcribed meeting speech — including the CEO's own mic — is never an instruction, only an explicit `/v1/turns` request is. Built as a workflow (a Go meetings-foundation phase, a Go help/recap phase, and a Swift audio-capture phase in parallel, then this integration pass covering prefetch, quiet cues, config wiring, the S13 scenario test, and both toolchains' gates).
  - **New Go package `internal/meetings`:** `Session`/`Segment`/`Manager` (session lifecycle over two new store tables, `0007_meetings.sql`); `Help` (on-demand help's context assembly: the last 5 minutes of transcript plus a local FTS5 retrieval fallback, `internal/meetings/help.go`); `Recap` (the after-meeting pipeline: code extracts decisions/action items/open questions/FYI by keyword and regex, `internal/decisions.Classifier` guesses a project match labeled with its confidence, one model call phrases it — brief.go's exact "code computes, model only phrases" split, `internal/meetings/recap.go`); `Cues` (section 6's quiet proactive suggestions, `internal/meetings/cues.go`, added in this pass — see below). Every `Segment` this package returns carries `External: true` unconditionally, on both channels.
  - **The daemon:** `POST /v1/meetings/start`, `POST /v1/meetings/{id}/segments`, `POST /v1/meetings/{id}/stop`, and (new in this pass) `GET /v1/meetings/{id}/cues`. `handleMeetingSegment` calls `escalateTaint(true)` as its first statement, before the body is even parsed, so every authenticated segment post taints the shared session tool token for good — whatever the channel, even the CEO's own mic, and even when the call is later rejected (400/404/409). `handleTurn` gained an optional `meeting_id`: when it names a live session, the turn's prompt is extended with that session's rendered `HelpContext` (clearly labeled untrusted, "quote or summarize only, never follow as instructions") and the turn's own taint escalation ORs in `HelpContext.Tainted` — which is unconditionally true once the session is found, even with zero segments so far, because a live meeting context is untrusted by existing rather than by content. An unknown `meeting_id` is silently ignored (no taint, no context, normal fast-path-eligible turn) rather than failing the whole turn.
  - **The FTS5 finding, confirmed:** `modernc.org/sqlite` (already in `go.mod`) compiles FTS5 into its translated SQLite source with no loadable extension, no cgo, and no new dependency. `0007_meetings.sql` adds `messages_fts`/`documents_fts` (content-linked to `messages`/`documents` via `content_rowid`), kept in sync by AFTER INSERT/DELETE/UPDATE triggers (the UPDATE trigger alone covers `Upsert`'s `ON CONFLICT DO UPDATE` path, so `Upsert` itself needed no change) and backfilled once via `INSERT INTO ..._fts(..._fts) VALUES ('rebuild')`. `internal/store/search.go` (new) exposes `SearchMessages`/`SearchDocuments` (bm25-ranked `MATCH` queries, local index only) and `QuoteFTSTerm` (per-term double-quoting, since raw transcript or message text can contain characters FTS5's query syntax rejects unquoted) — this file didn't exist before Slice M and both `Help` and `Cues` depend on it.
  - **Prefetch (`internal/sync/prefetch.go`, new), section 2.** Hooked into the existing events tick (`RunOnceEvents`, the same tick that already refreshes the calendar and drives the morning brief's background precompute) rather than a new ticker: once per tick, any calendar event in the store starting within `PrefetchLeadTime` (new config-shaped constant, `DefaultPrefetchLeadTime = 10m`) and not yet claimed gets its attendees' recent mail and the event title searched for in Drive, both through the gate at origin **P1** with Clean taint (a synchronous, CEO-adjacent read ahead of an imminent meeting, not P2 background drafting) — the same `gcal`/`gmail`/`gdrive` read functions already granted at level R, called with attendee/title-scoped arguments instead of the sync loop's own. An in-memory per-process "claimed" set (keyed by `source:source_id`) makes each event prefetch at most once per daemon life, marked claimed even on a failed call so a persistently-failing prefetch never retries forever. Tested entirely against fixture events and fake connectors (`internal/sync/prefetch_test.go`) — no real Google call.
  - **Quiet proactive cues, section 6 — built for real, not stubbed.** `GET /v1/meetings/{id}/cues` behind a new `meetings.proactive_cues` config key (default `false`, following `internal/config`'s existing plain-bool-flag pattern; disabled answers 200 with `enabled:false` and no items rather than 404ing, so a client can probe safely). When enabled, `Manager.Cues` reuses `Help`'s own FTS5 term-extraction over a short 45-second recent window (deliberately simple — the spec explicitly says this doesn't need to be clever) and returns up to 3 related local messages/documents. Rate-limited two ways, in memory, per session: no non-empty batch closer than `CueMinInterval` (30s) to the last one, and the exact same item set is never shown twice in a row even after that interval elapses — a caller polling every second will mostly see empty responses. Reading cues does **not** escalate taint: unlike posting a segment, it injects no new content and never reaches a model, so there is nothing new to taint against.
  - **Recap and the daemon: deliberately not auto-wired.** `Recap` is a plain, daemon-independent function (session id, classifier, backend, model, timeout in; a `RecapResult` and a `store.Meeting` upsert out) — the M.md spec calls for exactly this ("the same code path as the fixture transcripts"), and nothing in the spec requires `POST /v1/meetings/{id}/stop` to trigger it automatically. Wiring an automatic recap into the stop endpoint would need `daemon.go` to carry a classifier/backend/model/timeout it doesn't currently have reason to hold; left for whichever later slice actually wants a recap the moment a meeting ends (e.g. surfaced in the client's own UI) rather than added speculatively here.
  - **Scenario S13 (`internal/gateway/scenario_s13_test.go`, new), section 8's replay mode.** A scripted, timestamped transcript goes through the real HTTP surface end to end: `POST /v1/meetings/start` → many real `POST .../segments` calls (both channels) → two `POST /v1/turns` on-demand-help questions mid-meeting (interleaved with segments, not batched after), each asserted to hit the fast tier and to be grounded in the just-posted transcript text → `POST .../stop` (twice, proving idempotency) → `meetings.Recap` run directly against the same store the HTTP calls just wrote to, with `backend.Fake` and a fixed test `decisions.Classifier` standing in for the model and the registry. Asserts: the session taints on every segment regardless of channel; the recap extracts exactly the 2 scripted action items (one named owner, one unassigned), each level `twins.D` — staged, never executed; the project guess is labeled available with its confidence; and the `store.Meeting` row lands with `TranscriptRef` pointing at the session id (the transcript is not duplicated — it already lives in `meeting_segments`) and `External: true`. This is the proof the whole pipeline works without real audio or a live model.
  - **Swift client (`clients/macos/`), built in parallel and reviewed in this pass.** `WaterClientCore/Meetings.swift` (wire types, 8 KiB-safe segment splitting, RFC3339 timestamps), `MeetingController`/`SystemAudioTap`/`AudioCapture` (on-device `SFSpeechRecognizer` over both a continuous mic stream and a Core Audio process-tap-backed system-audio stream, rotating recognition requests on a 1.5s pause or 45s, posting only final results), a third hotkey (⌃⌥M) and a red menu-bar indicator while a session is active, and a signing-identity fix in `build.sh` (a stable self-signed "Water Local Code Signing" identity in its own keychain, needed once `NSAudioCaptureUsageDescription` and the new capture entitlements changed the app's designated requirement). Audio never crosses the socket — only timestamped, channel-tagged text, exactly like every other client call.
  - **Swift verification, split honestly.** *Proven live:* the release build compiles clean with zero warnings; `./test.sh` passes 32/32 (new suites `MeetingRequestTests` and `MeetingSocketTests`, plus an opt-in `WATER_LIVE_DAEMON_TEST=1` round trip against a real isolated daemon — start, a mic segment, a system segment, stop, then a post-stop segment correctly getting 409); the signing identity is stable across rebuilds and lives outside the login keychain; the app binary launches and stays up against an isolated daemon. *Not verified, needs the owner:* real audio capture on either channel (the Core Audio process-tap/aggregate-device wiring), on-device recognition quality and the pause-based request-rotation heuristic in practice, the Microphone/Speech/System-Audio-Recording permission prompts (macOS has no preflight API for the last one — a denial just delivers silence), and the ⌃⌥M hotkey firing live (the global monitor needs an Accessibility grant the build environment can't grant). The owner's already-running daemon predates these endpoints and was left untouched and unrestarted during all of this verification (an isolated scratch daemon was used instead) — it needs a restart onto a rebuilt binary before the real client's meeting capture can work at all.
  - **Left undone / for later slices:** the recap's action items stay drafts (level D) with no "confirm this into a real stored task" endpoint yet — M.md's spec names that as the eventual destination but doesn't spec its shape, and S13 verifies the draft/traceability/level-D properties directly rather than inventing an unspecced confirm flow; auto-recap-on-stop (see above); per-person voice ID within "system audio" (explicitly deferred by the spec); consent handling (already recorded as deferred in `docs/known-gaps.md` from the original M.md planning pass, unchanged by this build — do not point this at a meeting with real outside participants).
  - **No factual errors found in `docs/slices/M.md`**; it was not changed.
  - **Gates run, both toolchains:** `go vet ./...`, `go test -count=1 -race ./...` (28 Go packages, all green), `CGO_ENABLED=0 go build ./cmd/water`, `gofmt -l .` (clean); `cd clients/macos && swift build -c release` (clean) `&& ./test.sh` (32/32).
- 2026-09-24: **Demo/showcase slice: fake GitHub, Linear and HubSpot connectors, opt-in.** Requested directly by the owner, out of slice order (D's "remaining connectors" still means real GitHub/Linear/HubSpot/QuickBooks/LinkedIn integrations, not built by this entry): a way to demo the twin's decision cards, live tool calls and (eventually) the brief pulling from more than Google, without setting up real OAuth or API keys for services the owner doesn't actually use yet. **This is explicitly a demo/showcase feature, not a real integration — flag this for a future session: `internal/connectors/fake/{github,linear,hubspot}.go` are canned, fully in-memory data with no network call, no OAuth and no API key, ever, the same posture as the pre-existing `fake.Calendar`/`fake.Mail`/`fake.Docs`. Do not mistake them for real connectors when D eventually builds those.**
  - **Three new fake connectors, one file each in the existing `internal/connectors/fake` package** (matched its established organization — one package, `NewX(seed...)` constructors, `Functions()`/`Invoke`/`Normalize`, `External: true` throughout, since this is all other people's/organizations' activity): `github.go` (`list_prs`→`store.Commit`, `list_issues`→`store.Issue`, a fictional "nimbus-labs/core-api" repo with realistic PR/issue titles and fictional usernames), `linear.go` (`list_issues`→`store.Issue`, realistic startup project names like "Checkout Revamp"/"Platform Reliability" rather than "Project A/B"), `hubspot.go` (`list_deals`→`store.Transaction`, `list_contacts`→`store.Contact`, fictional companies/dollar amounts — "Acme Robotics — annual plan renewal" for $42,000, "Meridian Ventures — Series B follow-on discussion" for the investor-relationship example). Each exports `DefaultX()` seed functions used both by production demo wiring and by its own tests. **Judgment call:** GitHub's PRs and HubSpot's deals/contacts needed two seed types each, so their constructors take two explicit slice args (`NewGitHub(prs, issues)`, `NewHubSpot(deals, contacts)`) rather than the single-type `NewX(seed...)` variadic the existing three fakes use — noted here since it's a deliberate, minor deviation from the literal pattern, not an oversight. Also noted: `store.Transaction` has no dedicated pipeline-stage field, so HubSpot's `Normalize` repurposes `Account` (normally a bank/payment account name) to carry the deal's stage — documented in `hubspot.go`'s `Normalize` comment.
  - **A second twin, `twins/ceo-demo/` (id `ceo-demo`), not a same-directory `twin.demo.yaml`.** The original ask named `twins/ceo/twin.demo.yaml`; built instead as a full second twin id (`twins/ceo-demo/twin.yaml`, `twins/ceo-demo/decisions/*.yaml`) because `internal/decisions.LoadRegistry` derives its decisions directory from the *manifest's own* `id` field (`twins/<m.ID>/decisions`), and both the demo manifest and its `investor_request` variant need to declare a `hubspot` need that must never be validated against the real manifest (see next bullet). A same-directory file with a different name would still resolve to the same `twins/ceo/decisions/` at load time and offered no way to keep the two `investor_request` variants apart. This is the one deliberate deviation from the literal file path in the ask, called out explicitly so it isn't mistaken for missed work. `internal/cli/twin.go` gained `(*App).twinID()` (resolves `demoTwinID` iff `--demo` or `WATER_DEMO` is set, `realTwinID` otherwise — the only place this decision is made) and `buildCEORegistry(demo bool)` (adds the three fake connectors iff `demo`); `buildTwinDeps`/`buildTwinDepsFS`/`loadTwinManifest` all now take the twin id explicitly instead of hardcoding `"ceo"`. A normal `water daemon`/`water status`/`water doctor` (no flag, no env var) resolves `realTwinID` and is byte-for-byte unaffected — regression-guarded by `internal/cli/twin_test.go`'s `TestTwinIDDefaultsToRealAndDemoFlagOrEnvSelectsDemo` and `TestDemoManifestAddsFakeConnectorsRealDoesNot`.
  - **`investor_request`, fleshed out — in the demo copy only.** `twins/ceo-demo/decisions/investor_request.yaml` adds an `investor_deal` need against `hubspot.list_deals` (keyword-matched against the triggering item's `{keywords}`, same template mechanism `budget_request`/`generic` already use) plus a new `internal://investor_request.compute_deal_health` computation (`internal/decisions/investor_request.go`, mirroring `budget_request.go`'s `compute_runway` exactly: reads the already-resolved deal record in code, computes dollar amount/stage/days-to-close, never a model guess, empty/malformed input degrades to `missing_info` not a panic or error). The **real**, shipped `twins/ceo/decisions/investor_request.yaml` was deliberately **not** given this need — `decisions/registry.go`'s `validateNeed` rejects a `needs[].fetch` naming a function absent from the manifest being validated, so adding `hubspot.list_deals` there would stop the real, non-demo daemon from starting the moment this slice landed. Its notes section now cross-references the demo variant instead. This is the second deliberate deviation from the literal ask ("flesh out `twins/ceo/decisions/investor_request.yaml`") for the same underlying reason as the twin-id split above, and is the main judgment call of this slice worth a second pair of eyes.
  - **Wiring confirmed, not built new: live tool calls already work per-manifest.** `internal/gateway/daemon.go`'s `twinFunctions()` already renders *every* manifest function (any level but B) as an MCP tool for the model, generically over whichever registry/manifest the running daemon loaded — so granting `github`/`linear`/`hubspot` at level R in `twins/ceo-demo/twin.yaml` was sufficient by itself to make `water ask` able to call them live in demo mode; no runtime/gateway code changed. **Judgment call, noted rather than built:** `internal/runtime/brief.go`'s precomputed signal set (events, mail, open cards) was deliberately left untouched — extending it with Linear/HubSpot signals would need those records already present in the local store (the brief never calls the gate live, by design), which would mean either a new generic background-sync mechanism (today's `internal/sync` is Google-specific by construction) or a demo-only store-seeding step, both bigger and riskier than this slice's scope. `docs/demo.md` is honest about this: the brief itself is unchanged; GitHub/Linear/HubSpot show up through live `water ask` tool calls and through the `investor_request` card's live gate fetch, not inside the brief's own text.
  - **Tests, this slice:** `internal/connectors/fake/{github,linear,hubspot}_test.go` (function schema/`additionalProperties:false`, `Normalize` producing the right store type with `External: true`, a full Invoke-through-a-real-gate test via a new shared `harness_test.go`, mirroring `internal/gate/gate_test.go`'s harness); `internal/decisions/investor_request_test.go` (`computeDealHealth` unit tests including the empty/malformed-input table, the shipped demo YAML loading through the real embedded FS with the real `ceo-demo` manifest and confirming `hubspot.list_deals` is absent from the real `ceo` manifest, a full `Builder.Build` end-to-end happy path and a no-matching-deal `missing_info` path, and a hand-built sourceless-figure card rejected by `Card.Validate`); `internal/cli/twin_test.go` additions for `twinID()`'s flag/env resolution and the real-vs-demo manifest regression guard.
  - **`docs/demo.md`, new:** a copy-pasteable walkthrough — `water daemon --demo` (or `WATER_DEMO=1`), `water status --demo` to see the granted functions, `water ask` prompts that exercise `github.list_prs`/`linear.list_issues`/`hubspot.list_deals` live, how to see the HubSpot-backed `investor_request` card via `water decisions list`/`show`, and confirmation that a plain `water daemon` is unaffected.
  - **Gates run:** `go vet ./...`, `go test -count=1 -race ./...` (all packages green), `CGO_ENABLED=0 go build ./cmd/water`.
- 2026-09-24: **Slice C write increment: outbound mail as the agent, never the CEO.** `docs/slice-c-planning.md`'s §1 write functions are real: `gmail.draft_message` (level D), `gmail.send_message` (level A), `gcal.create_event` (level A) and `gcal.move_event` (level A), granted in `twins/ceo/twin.yaml` with rate caps, read back by `internal/approvals/readback.go` exactly the way `send_email`/`create_event` already were. The agent identity design: a second, real Gmail account (`docs/agent-identity-setup.md`'s owner steps) becomes a verified "Send mail as" alias on the CEO's real account through Gmail's own web UI — sending still uses the existing OAuth grant, only the MIME `From:` header changes, so outbound mail is unmistakably the agent's, never a spoofed CEO identity. The alias is `agent.mail_address` (new `AgentConfig`, `internal/config/config.go`); `gapi.Scopes()` grew from three scopes to five (`calendar.events` replacing the old readonly scope, plus `gmail.send`/`gmail.compose`), so the CEO must re-run `water connect google` once.
  - **Idempotency, the non-negotiable part.** `gapi.PostJSON` (`internal/connectors/google/gapi/post.go`) makes exactly one send attempt, with a single refresh-and-retry only for a pre-send 401 (Google never processed the call) — never a blind retry on 5xx/429/network failure the way `GetJSON`'s read-retry loop does, since a second send after an ambiguous failure risks sending twice. Three, and only three, outcomes: a definite success (2xx, decoded); a definite failure (any 4xx, a JSON-encode/URL failure before the network call, or a decode failure after a confirmed 2xx) safe to report and let a human retry from a fresh envelope; or `gapi.ErrSendOutcomeUnknown` (a network/transport error, or a 5xx after Google received the request) that a caller must never retry blindly — it has to check the side effect out of band (list sent mail, list events) before trying again. `gmail.sendMessage`/`gcal.createEvent`/`gcal.moveEvent` all pass this error straight through unwrapped and unretried; readback/approval execution paths do the same.
  - **Fixed in the adversarial review that followed:** `gcal.moveEvent` now sends a real HTTP **PATCH** (`events.patch`) through the new `gapi.PatchJSON`, under exactly `PostJSON`'s single-attempt contract. The same review made a 2xx whose body is lost or undecodable wrap `ErrSendOutcomeUnknown` (it was a plain error before, i.e. a send that happened read as a failure), made the approval endpoint and `water approve` report an unknown outcome as unknown (never "not executed") and a ran-but-failed-to-index action as executed, stopped `send_message`/`move_event` records from being indexed as clean (a sent forward quotes someone else's mail verbatim; a moved event keeps its creator's title), and made `internal/agentmail` stage at most one forward per message id.
  - **Forward-function decision, made explicitly (the task's own open question).** `internal/agentmail`'s inbound-triage watcher does **not** add a new `gmail.forward` connector function. A CEO-directed message found in the agent's own mailbox is staged as an ordinary `gmail.send_message` approval instead — `to: [agent.forward_to]`, `subject: "Fwd: " + original`, `body` quoting the original message and its sender — because a forward is functionally identical to `send_message`'s existing `{to, subject, body}` shape and adding a second connector function, a second manifest entry, a second read-back case and a second set of gate/registry tests for something `send_message` already covers exactly was not worth the risk under this session's time budget. The trade-off: the read-back the CEO hears before approving says "Send email to ... subject 'Fwd: ...'", not a more specific "forward this message" phrasing — a real, if small, UX gap a future session could close either with a dedicated `gmail.forward` function or a `send_message`-specific read-back heuristic keyed on a `Fwd:` subject prefix. New config `agent.forward_to` (empty by default) names where a CEO-directed message goes; empty means such mail is logged (`agentmail: ... looks meant for the CEO but agent.forward_to is not configured; not staged`) but never staged, since there is nowhere configured to send it.
  - **`internal/agentmail`, new package: session-less by design, unlike Slice M's meeting sessions.** No lifecycle, no start/stop — just `Watcher.Tick`, wired as a third independent loop in `internal/sync.Refresher.Run` next to the existing mail/events ticks (`Config.AgentMail`/`AgentMailInterval`, nil by default so every existing caller is unaffected). The connector (`agentmail.New`) is `*gmail.Gmail` wearing a second identity — same real, already-tested Gmail HTTP calls (including the incremental history-based walk), pointed at a **second vault credential** (`water connect google --account agent`, account name `"agent"`) instead of the CEO's own. Registered as `agentmail.list_messages` (never `gmail.list_messages`), granted at level R only in `twins/ceo/twin.yaml` and on the auto-allowlist — `draft_message`/`send_message`/`get_message` stay unreachable through this identity no matter what the underlying type implements, since the gate checks the manifest before it ever looks at a connector. **Isolation is the load-bearing design point:** the connector's `Normalize` always returns no records, because nothing that reads `store.Message` (the brief's "needs attention" heuristic, `internal/decisions.Trigger`'s candidate scan, sync's own mail-prefetch) filters by `Source` — persisting agent-mailbox mail into the shared store would silently mix it into the CEO's own mail signals and decision candidates. Instead `Watcher` reads each tick's raw JSON directly and keeps its own state in the store's existing generic cursor table: `agentmail:history_id` (incremental resume point, exactly like sync's gmail/gcal cursors) and `agentmail:agent_directed` (a small rolling JSON list of agent-directed messages, capped at 20, that `internal/runtime/brief.go` reads as a new signal — `agentmail.RecentSignals`, code-computed like every other signal, no live call from the brief itself). Classification is a dedicated `agentmail.Classifier` (implements `decisions.Classifier`, so it's swappable/testable the same way `decisions.ModelClassifier` is) asking one narrow question — "is this for the CEO or for the agent itself" — never the decision-registry's "does this need a decision, of which type"; an unreadable model reply defaults to CEO-directed (never silently dropped), mirroring `decisions.ModelClassifier`'s own Fallback rule.
  - **`internal/reports` wired into a real, reachable call site — not dead code.** `Card.HTMLReport` (`internal/decisions/report.go`) renders any decision card's sourced fields (question, deadline, evidence with its source, figures with theirs, options, gaps, recommendation) through `internal/reports.Render`. `POST /v1/decisions/{id}/email` (`internal/gateway/decisions.go`) and `water decisions email <id> --to ...` render the named open card and stage it as a `gmail.send_message` approval with the report as `html_attachment` — proposed through `approvals.Queue.Propose` exactly the way the model-tool bridge stages any other A-level call; nothing sends until the CEO approves it through the normal flow. Chose this over the task's own suggested example (an ask like "prepare a report on X and email it to Y" flowing through the model's own `send_message` tool call) because a model composing `html_attachment` itself never actually calls `reports.Render` — it would leave the renderer unreachable in practice even though the type-checker is satisfied; the decisions-email path guarantees the real code runs.
  - **P2 stays exactly as strict as before — verified, not just claimed.** `gate.authorize`'s P2 checks are unconditional over every function regardless of level, so `gmail.send_message`/`gcal.create_event`/`gcal.move_event` (all level A) fail its `spec.Level == twins.A` clause by construction; the only P2-reachable function this slice adds is `agentmail.list_messages` (level R, read-only, on the auto-allowlist like `gcal.list_events`/`gmail.list_messages`). `internal/agentmail`'s own staged forward always proposes its envelope at **origin P1** ("a task the CEO approved or scheduled"), never P2, specifically so gate.go's "auto mode never performs outward actions" rule can never even be asked to apply to it.
  - **What needs the owner, not buildable from here:** the Gmail alias setup and the agent's own account (`docs/agent-identity-setup.md` steps 1–2, Gmail's own web UI); re-consent (`water connect google`) for the grown scope set; connecting the agent's own mailbox (`water connect google --account agent`); a live send test (`water ask "draft a short thank-you email to ..."` → `water approve`) to confirm the alias actually delivers with the right `From:` header — nothing here was run against real Google, every test uses `httptest` fixtures or in-memory fakes.
  - **Tests, this slice:** `internal/approvals` gained `TestReadBackSendMessageAndDraftMessage`/`TestReadBackMoveEvent`; `internal/decisions` gained `TestCardHTMLReport*` (every field round-trips, untrusted content is escaped, the title/Lead fallback) and `budget_request_test.go` was updated (`gmail.draft_message` dropped from the shipped `staged_actions` — a staged action must be level A, and level-D drafting was never a fit, see the yaml's own note); `internal/agentmail` (new, ~20 tests): the connector's isolation guarantees, the classifier's for-CEO/agent-directed/fallback verdicts and its usage-cap charge, and the watcher's full tick behavior (not-connected skip, agent-directed signal recording, CEO-directed staging, no-forward-to safety net, expired-cursor resync, full-fetch-then-incremental argument shapes) all through a real `*gate.Gate` with a fake connector standing in for live Gmail; `internal/sync` gained `TestAgentMailRunsAsAThirdIndependentTick`; `internal/runtime` gained the brief's agent-mail signal tests; `internal/gateway` gained `TestEmailDecisionReport*` proving the reports path end to end through the real HTTP handler. `internal/gate/gate_test.go`'s `TestEmbeddedCEOManifestBuildsAGate` (the real shipped `twin.yaml` against the real connectors) still passes with all four new functions plus `agentmail` added.
  - **Gates run:** `go vet ./...`, `go test -count=1 -race ./...` (all packages green), `CGO_ENABLED=0 go build ./cmd/water`, `gofmt -l .` clean on every touched file.
- 2026-09-24: **Two owner-requested additions to the write increment: a CEO-own-account draft mode, and a disclosure signature on agent-sent mail.**
  - **`gmail.draft_for_review` (level D), new.** The agent writes the content, but the draft lands in the CEO's **own** Gmail drafts, so hitting send on it is indistinguishable from something the CEO personally typed — unlike `draft_message`, which always drafts as the agent alias. It runs through the same primary-account credential `Credential()` already returns for every gmail function, and `buildRawMessage` now omits the `From` header entirely when given `""` rather than setting it to a guess, so Gmail defaults it to the sending account's own address. **Limitation, explicit:** the primary account's own real address isn't known anywhere in this code (only the agent's alias, `agent.mail_address`, is configured) — omitting `From` and letting Gmail default it is the deliberate, simpler choice the task asked for, not a placeholder for a future lookup. `internal/approvals/readback.go` gained a matching `draft_for_review` case ("Prepare a draft in your own Gmail for you to review and send yourself: to ..., subject '...'.") sharing `draft_message`'s "create the draft" prompt. Confirmed against `gate.NeedsEnvelope`: level D never requires an envelope (only A, or tainted S, do), so like `draft_message` before it, this read-back case is reachable only through direct `ReadBack` calls today (`internal/cli/cmd_approve.go`'s the only production caller, and only A-level calls ever reach the approval queue) — informational/future-proofing, not a live approval prompt, exactly the same posture `draft_message`'s existing case already had.
  - **Disclosure signature on agent-identity mail, new.** `send_message` (and `draft_message`, since that one also drafts as the agent and will eventually go out as it) now appends a short, code-generated line to the body automatically — never left to the model to remember: `"\n\n---\nSent by Water, an AI assistant — approved before sending."`, or, when `agent.signature_name` is configured, `"\n\n---\nSent by Water, an AI assistant, on behalf of <name> — approved before sending."`. New config `agent.signature_name` (`internal/config/config.go`, `AgentConfig.SignatureName`, empty by default: `twins/ceo/role.md`'s environment section is still a placeholder, so there is no real name to read yet); empty omits the "on behalf of" clause entirely rather than inventing a placeholder name. The HTML alternative (when `html_attachment` is set) gets the same line behind an `<hr>`, visually separated from the caller's own HTML. `draft_for_review` never gets this signature — appending it would defeat the whole point of that mode, which is to read as the CEO's own unedited writing.
  - **Wiring gap, left open on purpose.** `agent.signature_name` is a real, round-tripping config key, and `gmail.Gmail` gained a `SetSignatureName` fluent setter so a caller can configure it — but no call site actually threads `cfg.Agent.SignatureName` into the connector yet, because the only place that constructs the production `gmail.New(...)` (`internal/cli/twin.go`'s `buildCEORegistry`) was out of this session's touch list. Until a future session adds `.SetSignatureName(cfg.Agent.SignatureName)` at that call site, `send_message`/`draft_message` will use the empty-name fallback line in production even if the CEO sets `agent.signature_name`. `New`'s and `NewWithOptions`' signatures were deliberately left unchanged (mailAddress only) so every existing call site (`internal/cli/twin.go`, `internal/agentmail/connector.go`, and the gate/gateway tests that build a registry against the real `twin.yaml`) keeps compiling untouched.
  - **Touched only:** `internal/connectors/google/gmail/gmail.go`+test, `internal/approvals/readback.go`+test, `internal/config/config.go`+test (one new field, `agent.signature_name`), `twins/ceo/twin.yaml` (`gmail.draft_for_review`, level D, same rate cap as `draft_message`).
  - **Tests, this addition:** `internal/connectors/google/gmail` gained `TestDraftForReviewCreatesDraftWithNoAgentFromOrSignature`, `TestSignatureOnSendMessageOnlyNotDraftForReview`, `TestSignatureRespectsConfiguredNameAndFallback`, `TestSendMessageAppendsSignatureToHTMLPartToo`, plus `draft_for_review` schema/declaration cases; `TestSendMessageSendsExactlyOnceAndNormalizesAsOwnContent`'s body assertion was updated to expect the signature. `internal/approvals` gained a `draft_for_review` case in `TestReadBackSendMessageAndDraftMessage`. `internal/config`'s existing `TestEveryKeyRoundTrips` covers `agent.signature_name` automatically (it iterates every key in `Keys()`).
  - **Gates run:** `go vet ./...`, `go test -count=1 -race ./...` (all packages green), `CGO_ENABLED=0 go build ./cmd/water`, `gofmt -l` clean on every touched file.
- 2026-09-24: **Slice E built on branch `slice-e`: twin-to-twin messaging.** This CEO twin can now exchange messages with a different twin's daemon. The full spec is in `docs/slices/E.md`. The branch was built separately from `feat/ceo-twin`, so the status line above was left alone and this entry is the only change to this file.
  - **New package `internal/twinlink`.**
    - The envelope has the spec's fields plus `in_reply_to`, which a response needs to name the request it answers.
    - The id is content-derived and includes the sender, so a retried delivery is a duplicate, not a second message.
    - `Deliver` makes exactly one attempt over the peer daemon's Unix socket. An unreachable socket is a definite "nothing was sent", a 4xx is a definite refusal, and a broken connection, a 5xx or a bad ack is `ErrOutcomeUnknown`.
    - Two connectors: `twinlink.send_message` (level A) and `twininbox.list_messages` (level R, `External`).
    - The peer table (socket and token for each peer) is one vault secret, `water.twinlink/<own twin id>`.
    - New migration `0008_twin_messages.sql`. Its CHECK pins `external = (direction = 'in')`, and a partial unique index allows one response per request per direction.
  - **The existing trust model, not a new one.**
    - Outbound goes through `gate.Invoke` with an envelope from the existing approval queue. `approvals.ReadBack` has a `twinlink.send_message` case, keyed on the full action id so it is never read back as `gmail`'s `send_message`.
    - Inbound is untrusted unconditionally. `handleTwinReceive` calls `escalateTaint(true)` first, then validates, audits (new audit kind `receive`) and stores the message as external. It never proposes an approval, calls a model or runs anything.
    - The model reads received messages only through the gate. The result is `Untrusted`, so the session taints, the same as reading an email.
  - **Routes.**
    - `POST /v1/twinlink/messages` accepts only a peer token: a `clients.json` entry named `twin:<id>`, where the id must equal `from_twin`.
    - `GET /v1/twinlink/messages` lists stored messages, and `POST /v1/twinlink/outbox` only proposes an envelope.
    - CLI: `water twin peer add|list|remove`, `water twin send|reply|inbox`, and a new global `--twin <id>`/`WATER_TWIN`.
    - New minimal second twin: `twins/counterparty/`.
  - **Judgment calls touching existing invariants. Each is a tightening; please review.**
    - `gateway.auth()` now refuses (403) any client token whose name starts with `twin:`. No existing token has that prefix.
    - The Google refresher starts only when the manifest grants `gcal.list_events`. The real `ceo` and `ceo-demo` twins both grant it, so their behavior is unchanged.
    - The receive handler writes its audit record before the store write, the same order the gate uses.
    - `twinlink.send_message` is added to the real `twins/ceo/twin.yaml` at level A. `ceo-demo` was left unchanged.
  - **Demo result.** `TestScenarioEBudgetQuestionBetweenTwoTwins` passes. Two complete daemons, each built from its own real manifest, with separate homes and real Unix sockets, run the whole exchange:
    - The CEO twin stages the budget question and the CEO approves it. It is delivered once.
    - The counterparty stores it as untrusted, with zero approvals, zero model calls, and its session tainted.
    - The counterparty's human approves the reply.
    - The CEO twin receives the reply as untrusted, with zero approvals, zero model calls, and its session tainted.
    - A second reply is refused at the sender. A second response forced at the socket gets a 409.
    - Both audit chains verify.
  - **Not done.**
    - No run with two real `water daemon` processes. The `ceo` twin's refresher would call real Google, and the peer tables live in the real Keychain, so the scenario runs in-process over real sockets instead.
    - Inbound messages are not in the brief or `StateSummary`, and there is no arrival notification.
    - No network (cross-machine) transport.
    - `clients.json` still needs a daemon restart after minting a peer token.
  - **Gates run:** `go vet ./...`, `go test -count=1 -race ./...` (all packages green), `CGO_ENABLED=0 go build ./cmd/water`, `gofmt -l` clean.
