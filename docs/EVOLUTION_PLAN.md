# Evolution plan: council to CEO twin

Status: **Slice C-base done (2026-09-24). Next: full-codebase review.** Slice B shipped an interim macOS client (menu-bar app, text pop-up bar, push-to-talk voice) and Slice C-base built the decision-registry/generic-frame/classification-trigger layer against it. Slice M (live-meeting assistance) stays blocked, unstarted, per the owner's own sequencing. Context: `docs/CONTEXT.md`.

## Constraints decided with the owner
- **Zero metered spend.** Model calls go through the Claude subscription (`claude` CLI) only. Voice is free and on-device: Apple `SFSpeechRecognizer` (on-device recognition) and `AVSpeechSynthesizer` / `say`. Twilio, the X API and paid speech services are deferred.
- **Latency comes from architecture.**
  - A warm `claude` stream-json process stays running between turns.
  - Replies are spoken sentence by sentence as they stream.
  - Schedule, brief and approvals questions are answered from the store without a model call.
  - Longer work gets an instant "on it" and runs in the background.
  - Targets: about 1s for state-backed answers, 1.5–3s when the model is needed.
- **Subscription usage windows replace a dollar budget.** Auto mode (P2) gets a per-window call cap, fed from `backend/ratelimit_store.go`.
- **Execution:** one remote cloud run per slice, reviewed before the next. Opus runs A1 (the security core). Sonnet runs the rest.

## Where the prompt's assumptions differ from the repo
- There is no Steve Jobs material in the repo or its history. "Persona material" means the CEO's `soul.md`, `experience.md`, `reasoning.md` and `.index.json`.
- There is no Researcher role. The dormant roles are COO, CTO and Design.
- Nothing streams today. `backend/exec.go` buffers the whole CLI output, and `backend/api.go` doesn't stream.
- There is no daemon, no Keychain code and no connector layer. The only Unix socket is the per-session approval broker.

## Keep / move / retire

**Amended by the owner, 2026-09-23 (during A2):** the council is **deleted
outright, not kept dormant**. Water is a single personal agent. The table
below reflects what actually happened, not the original A1-era plan.

| Existing | Fate |
|---|---|
| `agents/` (all four roles: souls, experience, reasoning, skills, memory seeds, role.yaml, `.index.json`) | **Deleted**, not archived. The CEO's role — responsibilities and environment, not a personality — lives in `twins/ceo/role.md`. Persona files survive only in git history. |
| `orchestrator` (executor, checkpointer, both routers, the state graph, `AgentMessage`/outbox) | **Deleted entirely**, including the executor and checkpointer. Pipelines (brief, recap, packets) will be plain Go functions in a later slice, not a state graph. |
| `agent` (`Assemble`, `call`, `RunTurn`, `Consult`, `ceoNode`/`cooNode`/`specialistNode`) | **Deleted.** `internal/runtime` (built in A2) was written fresh against the twin/gate/store shape and never depended on this package, so nothing needed porting. |
| `roles`, `persona` (incl. the skills loader), `identity` (role_id/content_hash + the HMAC keyring), `experience`, `diagnose`, `dashboard`, `surface`, `trace` | **Deleted.** The audit log and session/turn events replace `trace`; there is no per-role anything left to register, sign, or diagnose. |
| `chat`, `editor`, `session` | **Deleted.** Both were reachable only through the deleted `run`/`build-site` commands and did not compile against a council-free tree; `water chat` is now a plain daemon-client REPL (`internal/cli/cmd_chat.go`), and transcript persistence is left for a later slice. |
| `backend` | Kept. Model tiers, `RunStream`, and a warm session process were added in A2. The API backend stays opt-in only. |
| `tools` | Trimmed to what the twin uses: the MCP server (now in twin mode, proxying connector calls to the daemon's gate), `WritePolicyFile`/`LoadPolicyFile`, and `ResolveWithinRoots`/`ExpandRoots` (kept for a future connector, unused today). Deleted: the interactive-workspace read/write/run/apply_actions/open_page tools, the `ApprovalBroker` (the daemon's queue replaces it), and the shell sandbox. |
| `gate`, `approvals`, `audit`, `store`, `connectors`, `vault` (Slice A1) | Kept, extended in A2: rate/usage windows and the audit anchor persist in the store; `water audit verify`/`repair`. |
| `memory`, `config`, `auth`, `voice`, `guards` | Kept. `memory` is not yet wired into the twin's runtime (a later slice); `config` still carries some now-unused per-role/orchestration knobs (harmless, not cleaned up); `guards` lost the council-only tests and kept/ported the rest (strict MCP config, `--tools ""`, metered-leak/`ScrubbedEnv`, the A1 gate guards). |
| `theme`, `layout`, `themes/*.yaml`, `water.ThemesFS()` | **Deleted in A3.** Unwired dead weight since the council was deleted in A2; nothing referenced them outside their own tests. |
| `run`, `orchestrate`, `build-site`, `diagnose`, `dashboard`, `persona`, `experience`, `skills`, `replay`, `debug dump`, `memory` (CLI) | **Deleted.** `/consult`, `/switch`, `/agents` went with the TUI that had them. |

The new layout follows the tree in CONTEXT.md. Two approved dependencies are added, both pure Go so the binary stays static: `modernc.org/sqlite` and `modelcontextprotocol/go-sdk` (MCP client only). The Keychain is reached through the `/usr/bin/security` CLI behind a `Vault` interface, so it needs no cgo.

## Slices and acceptance tests
Every phase must pass vet, test and a `CGO_ENABLED=0` build.

1. **A1 (remote, Opus): foundation.**
   - Build: `gate` with R/D/A/S/B, default deny, untrusted tagging, rate and usage caps; `approvals` (payload-hash envelopes, a persisted queue, code-generated read-backs, a deterministic yes/no matcher); `audit` (hash-chained, 0600, fails closed); `store` (SQLite, migrations, eight record types with source IDs); `connectors` (the contract plus a fake connector); `vault`.
   - Tests: an A-level call without an envelope is refused; an edit voids the approval; untrusted-derived parameters are refused at S; every call is audited and the chain verifies; caps trip; tokens never appear in prompts or logs.
2. **A2 (remote, Sonnet): daemon and conversation plane.**
   - Build: `water daemon` on a 0600 Unix socket with per-client tokens; streaming turns (cli, voice, text-bar); approvals, state and cancel endpoints; `water chat` and `water ask` as clients; a LaunchAgent install command; `RunStream`, the warm session process and deterministic fast paths; retire the council wiring.
   - Tests: streaming, cancel, bad-token rejection, socket permissions.
   - On the laptop: measure time to first token.
3. **A3 (done): Google Workspace (Calendar, Gmail, Drive/Docs/Sheets), read-only.**
   - Remote: code and fixture tests.
   - Laptop: an internal GCP app, OAuth consent, and the live test suite.
4. **A4 (remote): morning brief pipeline.** Ranking signals are computed in code, and the model only writes the text.
5. **B (laptop-heavy): voice and clients.**
   - A Swift menu-bar app with a push-to-talk hotkey and a text-bar hotkey. Replies are spoken sentence by sentence, and approvals are confirmed by voice.
   - Interim: a macOS Shortcut that calls `water ask`.
   - A Slack connector.
6. **C (remote): decisions and planning.** Playbooks (budget, investor, hiring) and packets with a readiness state; the meeting-recap recipe; the action-plan scheduler; the four validated templates; the P0/P1/P2 queue with auto mode; the learning log.
7. **D: remaining connectors.**
   - In order: transcripts (Meet first), GitHub, Linear/Jira, HubSpot, QuickBooks sandbox, LinkedIn, file creation.
   - Remote: code. Laptop: credentials.
8. **E (remote): manifest generalization and twin-to-twin loopback.** Includes a second twin that needs no code changes.

**Deferred:** Twilio and X (both cost money), project health rubrics, dependency linking, the department layer, a phone client, wake word, and barge-in.

**Deferred, tracked separately (owner's long-term concern, 2026-09-23): host the daemon off the laptop.**
Right now `water daemon` only does anything while the Mac is awake (macOS suspends background processes on sleep), so background sync and Gmail push (once built) only work while the laptop is up. That's fine for now but won't scale to "always available." The eventual fix: run `water daemon` on a small always-on machine (e.g. a Mac mini, since it still needs to run the `claude` CLI logged into the subscription), with the laptop, phone, etc. becoming clients that just talk to it over the network instead of running it locally. Not scheduled to any slice yet; revisit once the core agent loop (through Slice C) is solid.

## Log
- 2026-09-23: Step 0 done. Reasoning-layer work committed on `feat/build-site`, branch `feat/ceo-twin` created, and CONTEXT.md, CLAUDE.md and this plan written.
- 2026-09-23: **A1 done.** New packages: `store`, `audit`, `canon`, `twins` (with `twins/ceo/twin.yaml`, embedded), `gate` (+ `gate/permit`, `gate/internal/mint`), `connectors` (+ `connectors/fake`), `approvals`, `vault`. The council code is untouched.
  - **How the gate is made unbypassable.** A connector's `Invoke(ctx, permit.Permit)` gets its arguments and credential only by redeeming the permit (`p.Open()`), and it can do that once. Permits are minted in `internal/gate/internal/mint`, which Go's internal-directory rule lets only `internal/gate/...` import. `gate/permit` re-exports the type, so connectors can accept a permit but cannot make one. The gate also fails any call where the connector returned without redeeming. A guard test checks all of this, and also scans the source to confirm that only the gate imports the mint or calls `Invoke`, and that every connector redeems as its first statement.
  - **Other choices:**
    - Taint's zero value is "unknown" and counts as tainted.
    - A tainted call to an S function escalates to needing an approved envelope. Tainted R and D calls are allowed because nothing leaves.
    - A manifest may grant a function's declared level or tighten it to A or B, never loosen it. `gate.New` enforces this.
    - Approvals bind the sha256 of the canonical payload and must also match the action. A mismatched call voids the approval. Claims are compare-and-set, and a stored row whose payload no longer matches its hash is refused.
    - Every audit write comes before the effect it records, and an audit failure fails the action. An approval that can't be audited is reverted to denied.
    - `audit.Open` refuses to extend a chain that doesn't verify.
    - Rate and usage windows are in memory for now, so the A2 daemon holds them.
    - P2 model calls pause while the persisted subscription status isn't "allowed".
    - Added one audit kind, `propose`, for a new envelope entering the queue.
  - **Known limit:** truncating the tail of the audit log isn't detectable without an external anchor for the last hash. Consider one in A2.
  - **Verify on the Mac:** `WATER_KEYCHAIN_TEST=1 go test ./internal/vault`. It passed during the run, but check it from a normal login session, and check that reads by `/usr/bin/security` never prompt. Secrets must be single-line because they go in on stdin.
- 2026-09-23: **Owner decision, mid-A2:** the council is deleted outright, not kept dormant. Water is a single personal agent from here on; there is no persona archive. This replaced Phase 4 of `docs/slices/A2.md` and is reflected in the keep/move/retire table above and in `docs/CONTEXT.md`'s amendment.
- 2026-09-23: **A2 done.** `water daemon` (HTTP over a 0600 Unix socket, single-instance flock, bearer-token clients) now owns the gate, approvals, audit, store and model sessions; `water chat`, `water ask` and `water approve` are its clients. New: `internal/runtime` (context assembly from `twins/ceo/role.md` + a state summary, a phrase-based zero-model-call fast path for schedule/approvals/brief questions, sentence-by-sentence voice streaming), `internal/gateway` (the daemon itself, plus a Twin extension to `internal/tools` that proxies model-initiated connector calls from an MCP-serve child to the daemon's gate). `internal/backend` gained `RunStream`, `WarmSession` (one persistent `claude` process reused across turns, restarting on crash/system-prompt/model/tool-scope change or after 40 turns) and model tiers from `twin.yaml`. Deciding a pending approval "yes" now executes the action in that same call, exactly once. Gate rate/usage windows and the audit log's tail now persist in the store (`water audit verify`/`repair` round out A1's open items). Then the council was deleted per the owner's decision above: `agents/`, `orchestrator`, `agent`, `roles`, `persona`, `identity`, `experience`, `diagnose`, `dashboard`, `surface`, `trace`, `chat`, `editor`, `session`, and the CLI commands that only served them.
  - **The stable-token deviation.** The spec sketch implied a token per turn for the model's tool-proxy auth. That cannot work with a warm session: the MCP bridge child a warm process spawns reads its `--mcp-config` policy file once, at its own startup, and then serves every turn in that process's life — so the token baked into it cannot rotate per turn without forcing a process restart on every single turn, which defeats the warm session. A2 instead mints one long-lived, session-scoped proxy token, and escalates its taint (never resets it) the first time any turn's assembled context includes external content. This is the conservative direction to err in given the constraint.
  - **Verified once by hand against the real `claude` CLI** (never in tests, which use `backend.Fake` or a fake-CLI shell script): streaming deltas and voice-channel sentences, `--include-partial-messages` actually needed adding to the warm session's own args (a bug caught only by this manual check — the automated warm-session tests use a fake CLI that doesn't need the real partial-message shape), a model calling `fake_calendar__list_events` and `fake_calendar__create_event` over MCP, the `create_event` call landing in the approval queue, and approving it executing exactly once. Cold spawn vs. warm reuse measured on this Mac: about 1.47s vs. 0.77s for a trivial turn.
  - **Left undone / for later slices:** `water memory` has no replacement command and long-term memory is not wired into the runtime yet; transcript persistence for `water chat` (the old per-role `session` package was deleted, not replaced); `theme`/`layout`/three of the four `themes/*.yaml` are dead weight, not deleted; `config` still carries unused per-role/orchestration knobs; the model-tool bridge's "session taken as one unit" taint model should be revisited once P1/P2 scheduling (auto mode) exists and more than one logical conversation can be in flight.
  - **Verify on the Mac:** `water daemon install` and `launchctl` loading it (unit-tested only as plist rendering); that a second `water daemon` truly refuses to start when the first is running under launchd, not just in-process; time-to-first-token specifically (this run measured whole-turn latency, not first delta) for warm vs. cold.
- 2026-09-23: **A3 done.** The twin can now read the CEO's real Calendar, Gmail and Drive, read-only, in place of the `fake_*` connectors. Built as a workflow: one Opus foundation phase, three connectors and a cleanup pass in parallel, then this integration pass.
  - **New packages.** `internal/connectors/google/gapi`: one Keychain credential (`water.google/<account>`, single-line JSON) shared by all three connectors, an in-memory access-token cache keyed by a hash of the credential (refreshed ahead of expiry or once on a 401, with truncated exponential backoff on 429/5xx/rate-limited-403), the PKCE loopback `Authorize`/`Revoke` flow, and scrubbing (exact-string plus a regex over `ya29.*`/`1//*`/`GOCSPX-*`/`4/*`) so no token ever reaches an error, the audit log or a store row. `gcal` (`list_events`), `gmail` (`list_messages`, `get_message`), `gdrive` (`search_files`, `read_file`) — every function `External: true`. Docs/Sheets/Slides read through Drive's `files.export`, so no scopes beyond the three read-only ones are needed.
  - **Wiring.** `twins/ceo/twin.yaml` now lists only the six Google functions (level R, per-function rate caps); `auto_allowlist` holds just `gcal.list_events` and `gmail.list_messages`. The `fake_*` connectors left the production manifest entirely — they now live only in the gate's, guards', and gateway's own inline test manifests (those tests already defined their own manifests and needed no changes; two tests that loaded the *embedded* `twins/ceo/twin.yaml` — `TestEmbeddedCEOManifestLoads` and `TestEmbeddedCEOManifestBuildsAGate` — were updated to check the real Google functions instead of `fake_mail.*`).
  - **`water connect google --client-file <path> [--account] [--status] [--revoke]`** runs the loopback flow (prints the URL and opens it with `/usr/bin/open` on macOS), stores the credential, and never prints a secret. `--status` forces a live refresh; `--revoke` revokes at Google and deletes the Keychain entry. `water doctor` reports connected/not-connected by presence only (no network call, so `doctor` stays fast and offline-safe).
  - **Background sync (`internal/sync`).** The daemon starts one `Refresher` tied to its own shutdown context: once right after start and then every `sync.interval_minutes` (default 10), it calls `gcal.list_events` (today through +7 days) and `gmail.list_messages` (`newer_than:1d`) through the gate at origin **P2** with **Clean** taint. It checks the vault for the shared credential before calling anything, so an unconnected twin logs one line instead of a stream of gate denials every interval. Its function ids and the credential it checks for are both overridable, so tests exercise it entirely with the `fake_*` connectors and a memory vault — no network, no real Google.
  - **The taint fix.** A2 built `escalateTaint` (sticky, never resets) but never called it from the one place that matters most: `handleToolInvoke`, the model's own tool-call path. A3 makes an `Untrusted` gate result there escalate the session's stable tool-proxy token, so a model that just read an email is tainted for every later call in that session, not only for taint computed once at context-assembly time.
  - **The fast-path fix.** `scheduleAnswer` and `runtime.StateSummary` used to `List` the 500 newest records by `created_at` and filter by `start_at` in Go — a background sync backfilling an old event, or just enough other records accumulating, could push a today-starting event out of that window. Added `store.EventsInRange(ctx, s, from, to)`, a direct `start_at` range query with no dependency on insertion order, and pointed both call sites at it.
  - **Cleanup, done alongside this slice by a parallel agent:** `internal/theme`, `internal/layout`, `themes/*.yaml` and `water.ThemesFS()` deleted (dead since the council left in A2); `internal/config` and `internal/voice` collapsed to what a single CEO twin actually uses (no more COO/CTO/design voices; old config files with the retired orchestration/sessions/tools/skills/ui/telemetry/memory keys and per-role voices still load, silently dropping them).
  - **Left undone / for later slices:** incremental sync (Calendar's `syncToken`, Gmail's `historyId`) — A4 territory, since the morning brief needs exactly that kind of "what changed" signal; write functions (`send_email`, `create_event`) are C/D; Docs/Sheets-specific APIs beyond `files.export` (comments, cell ranges) are undone.
  - **Verify on the Mac:** the whole of `docs/google-setup.md` end to end on a personal Gmail account — the consent screen's "In production" + unverified path really does avoid the 7-day Testing-mode token expiry; `water connect google --status` after a real day has passed; `water daemon` running long enough to see two sync intervals fire and check `water audit verify` afterward; `water ask "what's on my schedule today?"` answering from real data with zero model calls; `water ask "summarize my unread email from today"` actually calling the Gmail tools (visible in the audit log) rather than refusing or hallucinating.
- 2026-09-24: **A4 done.** Incremental sync for both Google connectors, a split two-interval background refresher, and the morning brief pipeline. Built as a workflow: a store-foundation phase, the two connector extensions in parallel, then this integration pass.
  - **Owner decision, recorded so it isn't re-litigated: no Gmail push.** Google's Pub/Sub push notifications need a billing account linked to the (free) Google Cloud project, plus one more permission re-approval, for roughly a minute of latency gain over a 60-second poll. The owner explicitly chose fast polling over push on 2026-09-24. A4 adds no Pub/Sub, no new OAuth scope, and no new dependency — it stays inside the existing hand-rolled-REST, GET-only pattern.
  - **`gcal`/`gmail` incremental support.** `gcal.list_events` gains an optional `sync_token` (mutually exclusive with `time_min`/`time_max`); a successful call of either shape now returns `{"events": [...], "next_sync_token": "..."}` instead of a bare array, and a 410 from Google becomes the typed sentinel `gcal.ErrSyncTokenExpired`. `gmail.list_messages` gains an optional `since_history_id`, walking `users.history.list` (`historyTypes=messageAdded`) instead of searching; output becomes `{"messages": [...], "history_id": "..."}`, and a 404 on a too-old `historyId` becomes `gmail.ErrHistoryTooOld`. Both changes are additive when the new argument is omitted (the underlying HTTP request is unchanged), but the output shape change is a deliberate, unavoidable break of the old bare-array bytes, needed to surface the cursor to callers — `Normalize` was updated for both, and both packages' full existing test suites still pass.
  - **Cursors (`internal/store`).** New migration `0003_sync_cursors.sql`: `sync_cursors(key, value, updated_at)` and `briefs(day, text, created_at)`, with `GetCursor`/`SetCursor`/`DeleteCursor` and `GetBrief`/`SetBrief` mirroring `gatestate.go`'s existing upsert-by-key pattern. Cursor keys follow a `connector:scope:field` convention (`gmail:history_id`, `gcal:primary:sync_token`); `briefs` is keyed by local calendar day (`YYYY-MM-DD`).
  - **The gate's redaction was silently discarding typed sentinel errors.** `gate.Invoke` scrubbed a failed connector call's error with `errors.New(redact(...))`, which drops the original error's identity — so `errors.Is(err, gcal.ErrSyncTokenExpired)` (or any other connector sentinel, pre-existing ones included) never matched the error `gate.Invoke` actually returns, contrary to A3's assumption that redaction "only strips credentials, not typed sentinel errors." Fixed with a small `scrubbedError` type that returns only the redacted text from `Error()` (so a credential still can't resurface through formatting) but implements `Unwrap` to keep the original error reachable for `errors.Is`/`errors.As`. Caught and fixed in this integration pass, since both connector-extension agents flagged it as a blocker for the sync loop's planned fallback logic but were (correctly) scoped away from touching `internal/gate` themselves.
  - **`internal/sync` split into two independent tickers.** `MailInterval` defaults to 60s (Gmail's per-user quota — 6000 units/min, ~2 units per `history.list` — makes this trivial); `EventsInterval` keeps the existing 10m default (`sync.interval_minutes`; new `sync.mail_interval_seconds` for mail). Each tick loads its own stored cursor, calls its function with it, and on success persists whatever cursor field the response carries. On the connector's expired-cursor sentinel (matched via `errors.Is` through the gate), the stored cursor is deleted and logged; the next tick is then naturally cursor-less, does a full fetch, and re-seeds a fresh cursor from that response — the same fallback the connectors already do unprompted, just triggered by an empty cursor rather than special-cased in the loop.
  - **Morning brief (`internal/runtime/brief.go`, new).** Signals — today's events, new messages since yesterday (by `created_at`, via the existing generic `store.List`), a simple non-model "needs a reply" heuristic (excludes obviously bulk/no-reply senders, then looks for a question mark or a short list of urgency/deadline phrases — documented in code, not meant to be exhaustive), and the pending-approval count — are all computed in code; one `backend.Request` asks the model only to phrase them, instructed never to invent a fact outside the signal block. Cached per local day in the new `briefs` table. `ComputeAndCacheBrief` is a compute-once guard (an in-memory per-day wait map): a background precompute and an on-demand ask racing each other share one backend call rather than each starting their own, verified by a test that fires several concurrent callers and asserts the fake backend saw exactly one. `fastpath.go`'s morning-brief branch calls it — the one deliberate, clearly-commented exception to "the fast path never calls the model." Background precompute reuses the events tick (no new goroutine): once local time is past `brief.ready_after` (default `07:00`) and nothing is cached yet for today, it calls the same function the on-demand path does.
  - **Taint.** Because a cached brief might have been computed by the background precompute long before anyone asks for it, taint is recomputed fresh on every request (cheap: local store reads only) rather than stored alongside the cached text, and escalates the session exactly like a normal turn's tainted context does. This needed one small, deliberate exception to "only touch the files this task named": `runtime.Env` gained an `OnTaint func(bool)` field, wired in `gateway/daemon.go`'s `baseEnv()` to the existing (private) `escalateTaint`, since the fast path has no other way to reach that mechanism.
  - **Left undone / for later slices:** the brief's own "needs attention" heuristic is intentionally simple and non-exhaustive; long-term memory still isn't wired into the brief's signals (it only reads the store); the background precompute's "past ready_after" check uses the sync loop's own clock (`Config.Now`, real time in production) rather than anything more sophisticated (a change in system timezone, a laptop asleep through the whole ready window, etc. are not specially handled — the next tick after wake still checks and computes). Nothing here rechecks the sync loop's assumption that Gmail keeps history for "at least a week" and gcal's `syncToken` lifetime against a real long-idle client — only fixture-driven 404/410 behavior is tested.
  - **Verify on the Mac:** `water daemon`, send a test email, confirm `water ask "summarize my unread email from today"` picks it up within about a minute instead of ten; `sqlite3 ~/.water/water.db "select * from sync_cursors;"` shows a `gmail:history_id` row after the first successful sync; `water ask "what's my morning brief"` computes the first time (a few-second pause) and is instant on a second ask; `water audit verify`.
- 2026-09-24 (overnight, docs only): **Owner amendment for Slice C and a new Slice M, specs written, no code.** The owner's amendment extends Slice C with a flexible decision-type registry and a generic decision frame, and adds Slice M (live-meeting assistance via the Swift client's on-device transcript, never audio, to the daemon). Their own sequencing is explicit — A4 (done) → B → C-base → M, "do not start C or M until the slice before it is verified" — and Slice B itself is still not built (an interim macOS Shortcut, hotkey → dictate → `water ask --voice`, is the only voice path that exists today; the real Swift menu-bar app, text pop-up first then voice, has not been started). This session therefore wrote specs and planning docs only, no Go code:
  - `docs/slices/C.md` — the decision-type registry (`twins/ceo/decisions/*.yaml`, validated at startup), the always-present `generic` frame, the `Classify` triage interface, the decision card shape, and the two shipped types (`generic`, `budget_request`) plus stub registry entries (`investor_request`, `inbound_decision`). Marked blocked on B.
  - `docs/slices/M.md` — the Swift-client/daemon split (system+mic audio and on-device STT stay client-side; only timestamped text segments cross the local API; the daemon never receives audio and never writes it to disk), the meeting-session lifecycle, prefetch at gate level R, a local SQLite FTS5 index, on-demand help as a P0 fast-model request, quiet proactive cues behind a default-off flag, the after-meeting recap reusing the fixture-transcript code path, and a replay mode via the same segments endpoint (scenario S13). Marked blocked on B and C-base.
  - `docs/slice-c-planning.md` — the exact write functions C's next increment needs (`gmail.draft_message`/`send_message`, `gcal.create_event`/`move_event`), the added OAuth scopes (`gmail.compose`, `gmail.send`, `calendar.events` replacing `calendar.events.readonly`), and a worked example showing the first write call would exercise the approval queue and `approvals.ReadBack` — which already special-cases `send_email`/`create_event` by name today, unused until C ships. Also records the P2 gate decision as **decided, not open**: `gate.authorize`'s existing P2 branch already restricts auto mode to allowlisted R/D functions and never level A, by construction, with no code change needed; this stays in force through C. Proposes a lightweight scenario harness (a new test-only `internal/scenario` helper, not a framework) and expands `twins/ceo/role.md`'s existing environment stub into a Company/Projects/People/Budget-structure template with placeholder values. Works out one example scenario (a conference speaking invite) that exercises the generic frame end to end.
  - **Finding: no scenario-numbering convention exists in this repo.** Searched `PROGRESS.md` and the archived decision logs for "scenario S<N>" or a numbered gate-test list; nothing matches. The amendment's "scenario S8" and "scenario S13" are used exactly as given, as this document's own new identifiers, not as continuations of a pre-existing numbered list.
  - **Finding: FTS5 works with the pure-Go driver already in use.** `modernc.org/sqlite` (v1.59.0, already in `go.mod`) compiles FTS5 into its translated SQLite source by default — no loadable extension, no cgo, no new dependency needed for Slice M's local text index.
  - **Housekeeping, same session:** `docs/architecture.md` and `PROGRESS.md` rewritten for the single-twin daemon (the four-role-council content they described no longer exists in the tree); `docs/known-gaps.md` rewritten for the twin's actual current deferrals (Slice C's standing-grants/probabilities/playbook-learning/classifier deferrals, Slice M's deferred consent handling), with its old persona-coverage content archived; `docs/decisions.md`, `docs/tools.md`, `docs/voice.md`, `docs/judgment-pass-v2.md`, `docs/persona-audit-prompt.md`, and both `docs/reasoning-layer-*-prompt.md` files moved to `docs/archive/` (via `git mv`, history preserved) since they describe the deleted council, orchestrator, or TUI and would otherwise mislead a future session.
  - **No .go files touched.** Gates run: `go vet ./...`, `go test -count=1 ./...`, `CGO_ENABLED=0 go build ./cmd/water`.
- 2026-09-24: **Slice B, interim: the macOS client.** `clients/macos/` is a Swift package built without Xcode (`build.sh` → an ad-hoc-signed `Water.app`; `test.sh` → swift-testing, since XCTest needs full Xcode). It has a menu-bar drop icon, a ⌃⌥Space text pop-up bar (`text-bar` channel, deltas streamed), and ⌃⌥V push-to-talk (on-device `SFSpeechRecognizer` only, with each `sentence` event spoken as it arrives). The HTTP-over-Unix-socket client is hand-rolled, and the token lives in the Keychain (`water.client`/`macos`). Verified against the live daemon over the socket. The hotkeys, mic, speech and TTS need the owner's permission grants. Two daemon findings: (1) clients.json is read only at daemon startup, so newly minted tokens 401 until a restart, and the app falls back to the `cli` token until then; (2) `approval_required` is defined but never emitted. Details and the owner's steps are in `docs/slices/B.md`. Still open in B: voice approvals, the Shortcut, and Slack.
- 2026-09-24: **Slice C-base done.** `docs/slices/C.md`'s decision registry, generic frame, classification triage and card builder are now real code, wired into the running daemon, the CLI and the morning brief. Built as a workflow: an `internal/decisions` foundation phase, `budget_request` (the one type exercised end to end) and the classification-trigger/store-cache/stub-types piece in parallel, then this integration pass.
  - **New package `internal/decisions`:** `Registry`/`LoadRegistry` (validates `twins/<id>/decisions/*.yaml` against the manifest — bad YAML, an unknown key, a `needs[].fetch` naming a function the manifest doesn't grant at level R, a duplicate id, and more all fail loudly, exactly like `twins.Load` does for a bad `twin.yaml`); the built-in `generic` frame; `Classifier`/`ModelClassifier` (one fast-tier model call over the registry's `id`+`trigger` index only); `Triager` (in-memory cache, keyed by the item's own `Source`+`SourceID`); `Card`/`Builder` (every number and evidence source traces to a connector result or a registered `internal://` compute function — the builder quarantines anything that doesn't and downgrades `Ready` to `missing_info` rather than let an unsourced figure through); `Phraser`/`ModelPhraser` (the only thing a model may write is prose, and even that is checked field-by-field against the card's own facts before being kept). Shipped types: `generic` (always present, a Go literal), `budget_request` (a real Drive-spreadsheet CSV parse computing runway/burn in code — `internal://budget_request.compute_runway`), and two schema-valid stubs, `investor_request`/`inbound_decision`, proving the registry holds more than one type without building either out.
  - **The design gap the specs left open, filled: WHEN Classify() runs, on WHAT.** Classifying every synced record would be wasteful and expensive. The rule built here: an item is a classification candidate only if `internal/runtime/brief.go`'s existing "needs attention" heuristic already flags it (the same cheap, code-only signal the morning brief already uses to call out messages) — nothing else is ever classified. `decisions.Candidate`/`NeedsAttention` (`internal/decisions/attention.go`) is a deliberate, documented byte-for-byte copy of `brief.go`'s private `needsAttention`, not a call into it: `internal/decisions` cannot import `internal/runtime` (this slice makes `internal/runtime` import `internal/decisions` for the new open-cards signal, so the dependency has to run one way), and `brief.go`'s heuristic is unexported. **Left for a later pass:** delete `brief.go`'s private `needsAttention` and have it call `decisions.Candidate` instead, collapsing the two copies into one — natural to do at the same time something next touches `brief.go` anyway. Each candidate is then classified at most once, ever, via two layered caches: `Triager`'s in-memory cache (dedups within one process) in front of `StoreCache` (`internal/decisions/trigger.go`), which persists the verdict in a new `decision_classifications` table (migration `0004`) keyed by `(Source, SourceID)` — a daemon restart never re-classifies an item it already has a verdict for. `Trigger.Run` only ever persists the *classification verdict*, never the card itself: a card is always rebuilt fresh from current gate/connector state, so a rebuilt runway figure, a newly-arrived comparable request, or a fixed placeholder Drive id all show up on the next look without needing any cache invalidation.
  - **Wiring into startup, the CLI and the brief (this integration pass).** `internal/cli/twin.go`'s `buildTwinDeps` now also calls `decisions.LoadRegistry` right after `twins.Load`, before the store or audit log ever open, so a malformed `twins/ceo/decisions/*.yaml` stops the daemon exactly as a bad `twin.yaml` does (`buildTwinDeps` was split into a thin default-path wrapper and a testable `buildTwinDepsFS(fsys fs.FS)`, so this is covered by a test against a fixture tree rather than only by inspection). `buildDecisionsTrigger` (same file) builds the one shared `*decisions.Trigger` — a fast-tier `ModelClassifier` wrapped in a durable `StoreCache`, feeding a `Builder` over the real gate — once backend selection happens in `cmd_daemon.go`'s `runDaemon`, and hands the same instance to both `gateway.Config.Decisions` and the background brief's `runtime.Env`. **CLI surface: `water decisions list` / `water decisions show <id>`**, mirroring `water approve`'s list-then-act shape: `list` hits a new `GET /v1/decisions` daemon endpoint (runs `Trigger.Run` over today's candidates, returns cards ranked by `decisions.Rank` — severity, then deadline), `show` renders one in full with `Card.Render()`. **Morning brief:** `internal/runtime`'s new `DecisionSource` interface (`Run(ctx, now) ([]*decisions.Card, error)`, which `*decisions.Trigger` already satisfies structurally) lets `brief.go` compute a ranked open-cards signal the same way it computes events/messages — present only when `Env.Decisions` is set and returns at least one card, never forced in as an empty section, and an `Untrusted` card escalates the brief's taint like an external message already does.
  - **A nil-interface trap avoided on purpose.** `gateway.Config.Decisions` and the trigger passed into the brief's `runtime.Env` are concrete `*decisions.Trigger` pointers that can be nil; assigning a nil pointer straight into the `runtime.DecisionSource` interface field would produce a non-nil interface wrapping a nil pointer, so `env.Decisions != nil` checks would misfire even though every call would then hit `Trigger.Run`'s own nil-receiver guard (which does handle it, defensively, but by erroring — not by omitting the signal). Both `baseEnv()` and `cmd_daemon.go`'s `briefEnv` construction only assign the field when the concrete pointer is non-nil.
  - **Tests, this pass:** `internal/cli/twin_test.go` (a malformed decision-type file and an unlisted fetch function both fail `buildTwinDepsFS` before the real store ever opens); `internal/gateway/decisions_test.go` — the end-to-end proof the task asked for: a fixture message with no matching registered type produces a valid `generic` card over the real `GET /v1/decisions` HTTP path (real registry, real trigger, real gate), plus a no-registry-configured case reporting an empty list rather than erroring; `internal/runtime/brief_test.go` gained cases for ranking, the omit-when-empty rule, taint propagation, and a failing `DecisionSource` surfacing its error; `internal/decisions/rank_test.go` unit-tests `Rank`'s severity/deadline/stability ordering directly.
  - **Left undone / for later slices:** `budget_request.yaml`'s `budget_spreadsheet` need still names a placeholder Drive file id (`CONFIGURE_ME_...`) the owner must replace with their real runway spreadsheet's id — until then it blocks to `missing_info`/errors at the gate rather than failing silently, but the type won't actually reach `Ready` against real data first; the scenario-harness proposal and the `role.md` environment-section expansion from `docs/slice-c-planning.md` are planning only, not built; C's write functions (`gmail.draft_message`/`send_message`, `gcal.create_event`/`move_event`) are the *next* increment, not this one — every `StagedAction` this slice can produce is correctly `Actionable: false` ("not yet available") since none of those functions exist in the manifest yet.
  - **No factual errors found in `docs/slices/C.md` or `docs/slice-c-planning.md`** during this pass; neither was changed.
  - **Gates run:** `go vet ./...`, `go test -count=1 -race ./...`, `CGO_ENABLED=0 go build ./cmd/water` — all green.
